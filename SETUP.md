# 車両移動経費請求書（RouteSearch）設置・設定手順

対象バージョン：Ver 1.5 以降

---

## 1. ZIPの中身

| ファイル | 内容 |
|---|---|
| `index.html` | 画面本体 |
| `api/receipt.php` | 領収書AI読み取りの中継プログラム（Gemini APIを呼び出す） |
| `api/config.sample.php` | APIキー設定ファイルのひな形 |
| `api/.htaccess` | APIキーのファイルを外部から開けないようにする設定 |
| `README.txt` | この手順書 |
| `CHANGELOG.txt` | 変更履歴 |

- **APIキーを書いた `api/config.php` は ZIP に含まれません。** バージョンアップで上書きしても、設定済みのAPIキーは消えません。
- ルートの `.htaccess` / `.htpasswd`（Basic認証）もサーバーごとに設定済みのため ZIP には含めていません。

---

## 2. 初めて設置するとき

1. ZIPを展開し、`index.html` と `api` フォルダを、サーバーの RouteSearch フォルダにアップロードします。
   - FTPソフトによっては `.htaccess` が「隠しファイル」扱いで表示されません。隠しファイルを表示する設定にして、`api/.htaccess` も必ずアップロードしてください。
2. 「3. Gemini APIキーの作成手順」でAPIキーを作成します。
3. 「4. APIキーをサーバーに設定する」でキーを設定します。
4. ブラウザでサーバーのURL（例: `https://（サーバー）/RouteSearch/`）を開き、「🔌 接続テスト」を押して「接続OK」と出れば完了です。

> AI読み取りはサーバーにアップロードしたページでのみ動きます。パソコン上の `index.html` を直接開いた状態では使えません。

---

## 3. Gemini APIキーの作成手順

### 3-1. APIキーを作る（必須・無料）

1. ブラウザで **Google AI Studio のAPIキー画面**（ https://aistudio.google.com/apikey ）を開き、Googleアカウントでログインします。
   - 初めての場合は利用規約への同意画面が出るので、内容を確認して同意します。
2. **「Create API key（APIキーを作成）」** をクリックします。
3. プロジェクトの選択画面が出たら、**「新しいプロジェクトでキーを作成」** を選びます（すでにGoogle Cloudのプロジェクトがあれば、それを選んでも構いません）。
   - APIキーは必ず1つの「Google Cloud プロジェクト」にひも付きます。請求・利用上限はプロジェクト単位で管理されます。
4. 作成されたキー（英数字の長い文字列。通常 `AIza` で始まる）を **コピーして安全な場所に保管** します。

> ⚠ APIキーはパスワードと同じです。メール・チャット・Git・`index.html` などには絶対に貼らないでください。サーバーの `api/config.php` にだけ書きます。

### 3-2. 有料枠にする（本番運用では強く推奨）

キーを作っただけの状態は **無料枠** です。無料枠と有料枠の主な違いは次のとおりです。

| | 無料枠 | 有料枠 |
|---|---|---|
| 送った画像の扱い | Googleの製品改善に使われ、人が確認する場合がある | 製品改善に使われない |
| 利用上限（1分あたりの回数など） | 少ない（大量にまとめて読むと「利用上限」エラーが出やすい） | 大幅に多い |
| 料金 | 無料 | 使った分だけ（領収書1枚あたり1円未満の見込み。ダブルチェックで2回分） |

領収書には利用場所や日時が含まれるため、本番では有料枠をおすすめします。

1. Google AI Studio のAPIキー一覧（またはプロジェクト一覧）で、対象プロジェクトの **「Set up billing（請求先を設定）」** をクリックします。
2. 画面の案内に従って **お支払いアカウント（Cloud Billing）を作成またはひも付け** し、クレジットカードなどの支払い方法を登録します。
   - 最初に少額の前払いを求められる場合があります。
3. 完了すると、そのプロジェクトのキーが有料枠として扱われます（キーを作り直す必要はありません）。

### 3-3. キーの利用範囲を制限する（推奨）

万一キーが漏れたときの被害を小さくするため、キーを「Gemini API専用」にします。

1. **Google Cloud コンソールの認証情報画面**（ https://console.cloud.google.com/apis/credentials ）を開きます。
2. 画面上部のプロジェクト選択で、キーを作ったプロジェクトを選びます。
3. 一覧から対象の **APIキーの名前** をクリックします。
4. **「APIの制限」→「キーを制限」** を選び、**「Generative Language API」だけ** にチェックを入れて保存します。
   - Gemini APIの正式名称が「Generative Language API」です。
5. （任意）サーバーからインターネットに出るときのIPアドレスが固定なら、**「アプリケーションの制限」→「IPアドレス」** にそのIPを登録すると、そのサーバー以外からは使えなくなります。
   - IPアドレスはレンタルサーバーの管理画面やサポートで確認してください。**分からない場合は設定しないでください**（間違えるとAI読み取りが使えなくなります）。

### 3-4. 予算アラートを設定する（推奨）

1. Google Cloud コンソールの **「お支払い」**（ https://console.cloud.google.com/billing ）を開き、対象のお支払いアカウントを選びます。
2. **「予算とアラート」→「予算を作成」** をクリックします。
3. 対象のプロジェクトを選び、月の予算額（例：1,000円）と通知のしきい値（例：50%・90%・100%）を設定して保存します。

> 予算アラートはメールで知らせる機能で、利用を自動で止めるものではありません。

---

## 4. APIキーをサーバーに設定する

1. サーバーの `api` フォルダにある `config.sample.php` をコピーし、同じ `api` フォルダに **`config.php`** という名前で保存します。
2. `config.php` をテキストエディタで開き、次の行の `YOUR_GEMINI_API_KEY` を作成したAPIキーに書き換えて保存します。

   ```php
   'gemini_api_key' => 'YOUR_GEMINI_API_KEY',
   ```

   - 前後の `'`（シングルクォート）と、行末の `,` は消さないでください。
   - 文字コードは UTF-8 のまま保存してください。
3. 画面の **「🔌 接続テスト」** を押し、次のように表示されれば設定完了です。

   `接続OK：モデル gemini-3.8-flash／APIキー末尾 …（キーの最後の4文字）／サーバー Ver 1.5`

---

## 5. APIキーを変更するとき

キーの定期的な交換や、漏えいが疑われるときの手順です。

1. Google AI Studio（ https://aistudio.google.com/apikey ）で **新しいキーを作成** します（この時点では古いキーはまだ削除しません）。
2. サーバーの `api/config.php` の `gemini_api_key` を **新しいキーに書き換えて上書き保存** します。
   - `index.html` や `receipt.php` を変更する必要はありません。保存した直後の読み取りから新しいキーが使われます。
3. 画面の **「🔌 接続テスト」** を押し、「APIキー末尾」が **新しいキーの最後の4文字** になっていることを確認します。
4. 問題なければ、Google AI Studio または Google Cloud コンソールの認証情報画面で **古いキーを削除** します。

> 漏えいが疑われるときは、手順 2・3 のあとすぐに古いキーを削除してください。
> 新しいキーにも「3-3. キーの利用範囲を制限する」の設定をしてください。

### モデルを変更するとき

Googleが古いモデルの提供を終了した場合などは、`api/config.php` の `model` を新しいモデル名に書き換えます。
モデル名は Google AI Studio のモデル一覧で確認できます。変更後は「🔌 接続テスト」で確認してください。

---

## 6. バージョンアップの手順

1. 新しい `RouteSearch_v〇.〇.zip` を展開します。
2. `index.html` と `api` フォルダの中身（`receipt.php`・`config.sample.php`・`.htaccess`）を、サーバーの RouteSearch フォルダに **上書きアップロード** します。
   - `api/config.php`（APIキー）は ZIP に含まれないため、上書きされません。
3. ブラウザで **Ctrl + F5**（Macは Cmd + Shift + R）を押して強制的に再読み込みします。
4. 画面の見出しの **「Ver」表示** と、「🔌 接続テスト」の **「サーバー Ver」** が同じ番号になっていることを確認します。
   - 違う場合は「バージョンが違います」と表示されます。`api` フォルダの更新漏れがないか確認してください。
5. 変更内容は `CHANGELOG.txt` で確認できます。

---

## 7. うまくいかないとき

| 画面の表示 | 原因と対処 |
|---|---|
| サーバーに api/receipt.php が見つかりません | `api` フォルダの置き場所を確認（`index.html` と同じフォルダの中に `api` フォルダ） |
| サーバーの応答が正しくありません（PHPが使えるか確認） | サーバーでPHPが動いていない可能性があります。サーバーの管理画面でPHPが有効か確認してください |
| APIキーが未設定です | `api/config.php` がない、またはキーが `YOUR_GEMINI_API_KEY` のまま → 「4.」の手順で設定 |
| APIキーが無効か期限切れです | キーのコピー間違い、または削除済みのキー → 正しいキーを貼り直すか、新しいキーを作成 |
| APIキーにこのAPIの利用権限がありません | キーの制限で Generative Language API が外れている → 「3-3.」を確認 |
| Gemini API の利用上限に達しました | 無料枠の回数制限です。少し時間をおくか、有料枠（「3-2.」）にしてください |
| モデルが見つかりません | `api/config.php` の `model` を最新のモデル名に変更 |
| 送信データが空です（post_max_size） | ファイルが大きすぎます。PDFは5MB以下にしてください |
| パソコン上のファイルを直接開いた状態では… | サーバーのURLでページを開いてください |
| ドロップしたらブラウザで画像が開いた | 古いバージョンの画面が表示されています。Ctrl + F5 で再読み込みしてください |
