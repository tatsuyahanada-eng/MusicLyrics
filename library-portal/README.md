# ライブラリポータル

関連会社で共有する資料・プログラム・アプリの**更新履歴**を管理する Web アプリケーションです。
1 行 = 1 アイテムの一覧をクリックすると、そのアイテムの更新履歴（更新内容・日付・時間・対応者・
対象機能・修正したプログラム）が開きます。

- 構成：PHP 8 + MySQL 8（さくらのレンタルサーバを想定）
- 形態：PWA（ホーム画面に追加してアプリとして利用可能。HTTPS 必須）
- 権限：**管理者**（フルコントロール）／**閲覧のみ**（一般利用者）

## 設置手順

`docs/deploy-sakura.md` を参照してください。要点は次の 5 つです。

1. さくらのコントロールパネルで MySQL データベースを作成
2. phpMyAdmin で `sql/schema.sql` をインポート
3. このフォルダの中身を公開ディレクトリへ転送（`preview.html` は不要）
4. `includes/config.sample.php` を `includes/config.php` にコピーして接続情報を記入
5. `setup.php` で最初の管理者を作成 → **setup.php を削除**

## ファイル構成

| パス | 内容 |
|---|---|
| `index.php` | ライブラリ一覧（ログイン必須） |
| `login.php` / `logout.php` | ログイン・ログアウト |
| `settings.php` | 設定 — 利用者管理（管理者のみ） |
| `setup.php` | 初回のみ使用。最初の管理者を作成したら削除 |
| `api/items.php` | 一覧取得（GET）／アイテム登録（POST・管理者） |
| `api/updates.php` | 更新履歴の登録（POST・管理者） |
| `api/users.php` | 利用者の一覧・追加・変更・削除（管理者） |
| `api/password.php` | 自分のパスワード変更 |
| `includes/` | 設定・DB 接続・認証（ブラウザから直接アクセス不可） |
| `assets/` | CSS・JavaScript・ロゴ・PWA アイコン |
| `sql/` | スキーマとサンプルデータ |
| `docs/` | 設置手順・データベース設計 |
| `preview.html` | デザイン確認用の静的プレビュー（サンプルデータ表示・本番不要） |

## 権限

| | 閲覧・ダウンロード | アイテム登録 | 更新履歴登録 | 利用者管理 |
|---|:--:|:--:|:--:|:--:|
| 管理者 | ○ | ○ | ○ | ○ |
| 閲覧のみ | ○ | × | × | × |

権限は設定画面の行内スイッチでいつでも切り替えられます。
画面表示だけでなく API 側でも検証しており、URL の直接入力では操作できません。

## セキュリティ

- パスワードは `password_hash()`（bcrypt）で保存。5 回連続失敗で 10 分ロック
- 書き込み API は CSRF トークンを検証
- SQL はすべてプリペアドステートメント
- 出力は HTML エスケープ済み。ダウンロード URL は `http(s)` のみ許可
- `includes/` `sql/` `docs/` は `.htaccess` で直接アクセスを禁止
- ログイン・登録・利用者変更は `lp_audit_log` に記録
