# CLAUDE.md

このファイルは、このリポジトリでコードを扱う際の Claude Code (claude.ai/code) 向けのガイダンスです。

## 新規プロジェクト作成時のルール

**重要:** ユーザーが新しいプロジェクトを作成したい旨を伝えてきた場合、実装や作業を開始する前に、必ず以下を確認すること。

1. **リポジトリ名を必ず確認する。** 現在のリポジトリ (例: `MusicLyrics`) の中にそのまま新しいプロジェクトのファイルを作ってしまうミスが発生しがちなので、必ず `AskUserQuestion` などで以下を確認する:
   - 新しいリポジトリを作成するのか、既存のリポジトリに追加するのか
   - 新規作成の場合、リポジトリ名は何にするか
   - どの場所（ディレクトリ）に作成するか

2. **確認が取れるまで、ファイル作成やコード生成は行わない。** 特に現在の作業ディレクトリに関係のないファイルを書き始めないこと。

3. リポジトリ名が決まったら、明示的にそのリポジトリ / ディレクトリの中で作業を開始する。

### 判断の基準

- 「〜のアプリを作って」「新しく〜を作りたい」「〜のプロジェクトを始めたい」など、新規プロジェクトを示唆する発言があった場合は上記のルールを適用する。
- 既存ファイルの修正や、明らかに現在のリポジトリ内での作業依頼にはこのルールは適用しない。
