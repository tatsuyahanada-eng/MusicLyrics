# CLAUDE.md

このファイルは、このリポジトリでコードを扱う際の Claude Code (claude.ai/code) 向けのガイダンスです。

## 新規作成時のルール（例外なし・必須）

**最重要ルール:** 何かを「新規で作成する」場合は、**常に、例外なく**、作業を始める前にリポジトリ名と配置場所を確認すること。

このルールは以下のすべてに適用される（判断で緩めないこと）:

- 新しいアプリ / サービス / ツール / スクリプト / サイトを作る場合
- 新しい機能を単独のプロジェクトとして立ち上げる場合
- 既存プロジェクトとは無関係な検証コード・プロトタイプ・サンプルを作る場合
- ドキュメント / 素材（画像・PDF・マニュアル等）を単独の成果物として作る場合
- 「〇〇を作って」「〇〇を試したい」「〇〇のサンプルを」など、少しでも新規性のある依頼

### 手順（必ずこの順で行う）

1. **`AskUserQuestion` で以下を必ず確認する**（作業ディレクトリの推測や、現在のリポジトリへの追加を勝手にしない）:
   - 新しいリポジトリを作成するのか、既存のリポジトリに追加するのか
   - 新規作成の場合、**リポジトリ名は何にするか**
   - どのディレクトリに配置するか
2. 確認が取れるまで **ファイル作成・コード生成・ブランチ作成を一切行わない**。
3. リポジトリ名と場所が確定してから作業を開始する。

### やってはいけないこと（過去に発生した問題）

現状、`MusicLyrics` リポジトリの中に、音楽歌詞アプリとは無関係な多数のプロジェクト（音声書き起こしアプリ、ネットワーク診断、相撲情報サイト、交通費フォーム、SNS 素材、FX シグナル、タスク管理、せどり価格チェッカー、店舗 ID ツール等）がブランチ単位で混在している。**これは上記ルールを守らなかった結果であり、繰り返してはならない。**

- 現在の作業ディレクトリ（例: `MusicLyrics`）を「そのまま使えばいい」と勝手に判断しない
- 既存リポジトリに新規ブランチを切って別プロジェクトを始めない
- 名前を仮で決めて進めない（後で直すのが困難になる）

### このルールが適用されない場合

以下は「新規作成」ではないので、通常どおり進めてよい:

- 既存プロジェクトの明確なバグ修正・機能追加・リファクタ
- 依頼内容が現在のリポジトリの範囲内であることが文脈上明らかな場合

判断に迷ったら「新規作成」側に倒し、必ず確認する。
