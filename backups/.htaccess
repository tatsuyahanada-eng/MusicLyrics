# 完全バックアップ（パスワードハッシュ等を含む）への直接アクセスを禁止（Apache）。
# アプリからは api.php 経由（管理者ログイン必須）でのみ読み書きします。
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
