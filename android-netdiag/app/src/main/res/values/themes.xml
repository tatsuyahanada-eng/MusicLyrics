<resources>
    <!-- Base application theme. Compose draws the actual UI; this only styles
         the system window before/around the Compose content. -->
    <style name="Theme.NetDiag" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
