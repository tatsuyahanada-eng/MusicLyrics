<resources>
    <!-- Base application theme. Compose draws the actual UI; this only styles
         the system window before/around the Compose content. Dark window
         background so there is no white flash before Compose draws. -->
    <style name="Theme.NetDiag" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
