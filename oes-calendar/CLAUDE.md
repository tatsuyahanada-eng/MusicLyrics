# CLAUDE.md — OES入替作業APP

このディレクトリは「OES入替作業APP」プロジェクトです。詳細仕様は `SPEC.md` を参照してください。

## 最重要の前提：複数人で使うアプリである

**このアプリは会社の複数の担当者が、それぞれ自分の端末で使うことを前提に設計されている。
「一人用ツール」として単純化して考えないこと。**

- **設定（業態・時間帯・定型文・よく使う文など）は全員が同じ内容を使う。** 保存場所は
  index.html と同じ場所に置く **`settings.json` の1ファイルだけ**。起動時にこれを読み込み、
  全員が同じ内容で動く（`loadSharedSettings()`）。ブラウザの `localStorage` は、共有設定を
  読めなかったときの控えとしてのみ使う。
- **設定の変更は管理者だけが行う。** 設定タブは既定でロックされ、管理者パスワードを
  入れないと編集できない（`unlockSettings()`）。**パスワードの平文を `index.html` に書かないこと。**
  照合は `verifyAdminPassword()` が次の順で行う。
  ① `admin-auth.php`（あればサーバー側で照合。パスワードは `config.php` の中だけにあり、
     ブラウザからは見えない＝推奨） → ② `config.json` の `adminPasswordHash`（SHA-256、
     ソルトは `ADMIN_SALT` = `'oes-calendar:'`） → ③ 組み込みの `ADMIN_HASH_FALLBACK`（初期パスワード）。
  ②③はハッシュなので平文は読めないが総当たりは防げない。厳密に守るなら①か
  `deploy/.htaccess.sample` のBasic認証を併用する。初期パスワードは `deploy/config.php` を参照。
  解除時に入力された平文は `adminPw` に置き、共有設定の保存や手順書アップロードのPOSTに使う。
  **`sessionStorage`（キー`ADMIN_PW_KEY`＝`'oes-calendar-admin-pw'`）にも保存し、`lockSettings()`
  またはタブを閉じるまで残す**（利用者の指定：ロック解除中に画面を開き直しても
  ＝PWAの再起動やページ再読み込みでも、手順書アップロードのたびに毎回パスワードを
  聞き直されるのは不便、という要望で、当初の「メモリのみ・保存しない」方針から変更した。
  `restoreAdminPw()` が起動時に `isAdminUnlocked()`（`sessionStorage`の別キー）がtrueのときだけ
  読み戻す。`sessionStorage`はそのブラウザタブを閉じれば消え、他のタブ・他の端末には残らないため、
  「ロック中は誰でも見られる設定タブ」という前提のリスクを大きくは広げない、という判断）。
- **「担当者」という概念は廃止済み。** 担当者名・担当者定型文・担当者ごとのカレンダーURL・
  差し込み文字 `{担当者}` `{担当者定型文}` はすべて削除した。復活させないこと
  （旧い設定ファイルに残っていても `fillTemplate()` が空文字に置き換えて消す）。
- **Googleカレンダーからの予定の直接読み込みは停止中。** 利用者ごとにカレンダーが違い、
  共有設定と噛み合わないため、作業当日タブからは削除した。設定項目（Apps ScriptのURL・
  絞り込み文字・接続テスト・コード表示）だけは将来の再開に備えて「詳細設定」に残してある。

## プロジェクト概要

GoogleカレンダーへOES入替作業の予定を登録するための、単一HTMLファイルのWebツール。

| ファイル | 役割 |
|---|---|
| `index.html` | 本体。HTML/CSS/JS＋画面用の画像（data URI）を内包 |
| `manifest.json` | PWAのマニフェスト（**実ファイルであることが必須**。data URIや `.webmanifest` に戻さない） |
| `icon-192.png` `icon-512.png` `icon-512-maskable.png` | PWAアイコン（**ルートに置く**。`assets/` にも同じものがある） |
| `sw.js` | Service Worker。Androidで独立したアプリ（WebAPK）として登録されるために必要 |
| `manual.html` | 操作マニュアル（レスポンシブHTML。ブラウザの印刷でPDF化可） |
| `assets/welsys-logo.jpg` | ウェルシス株式会社ロゴ（manual.html が参照。index.html は同じ画像をbase64で内包） |
| `assets/device-printer.jpg` `assets/device-kitchen.jpg` | 対象機器の画像（マルチプリンター／マルチステーション）。manual.html が参照。index.html は縮小版をbase64で内包 |
| `icon-*.png` | PWAアイコン（192/512/180/512-maskable）。**`manifest.json` から実URLで参照する**（Androidでアイコンを取得するために必須）。180はapple-touch-iconとしてindex.htmlにdata URIで内包 |
| `apps-script/Code.gs` | Googleカレンダー連携用のApps Scriptコード（現在は停止中の機能。参考用に保持） |
| `settings-save.php` | 共有設定をサーバーに保存する任意のエンドポイント（PHPが動く場合のみ使われる。**index.htmlと同じ階層にそのまま置く＝リポジトリのこの場所が既に本番の置き場所**。手動でコピー・リネームする必要はない） |
| `admin-auth.php` | `config.php` を使ってサーバー側でパスワードを照合するエンドポイント（同上、そのまま置く） |
| `manual-upload.php` | 業態ごとの手順書・資料（画像・PDF）のアップロード用エンドポイント（同上、そのまま置く。保存先の `manuals/` フォルダは初回アクセス時に自動作成される） |
| `manual-delete.php` | 業態ごとの手順書・資料の実ファイルを削除するエンドポイント（同上、そのまま置く） |
| `manual-view.php` | PDFをダウンロード形式（`Content-Disposition: attachment`）で渡すエンドポイント（同上、そのまま置く。端末の既定のPDFアプリで開けるようにするため） |
| `deploy/.htaccess.sample` | Basic認証用サンプル（内容がサーバーごとに違うため手動設置。`.htaccess`として置く） |
| `deploy/config.php` | 管理者パスワードの置き場所（PHP。ここが唯一の平文。**秘密情報のため自動配布の対象にせず、手動で`config.php`として置く**。一度置けば以降のZIP更新で上書きされない） |
| `deploy/config.json.sample` | PHPが使えない場合のパスワード設定（SHA-256ハッシュ）。手動で`config.json`として置く |
| `deploy/.user.ini.sample` | PHP-FPM環境で `post_max_size` / `upload_max_filesize` を引き上げるサンプル（サーバーごとに要否が違うため手動設置。`.user.ini`として置く。任意） |
| `deploy/.htaccess-upload-limits.sample` | 上記が効かないmod_php環境向けの代替サンプル（`.htaccess`に追記する。任意。設置直後にサイトが表示できなくなったら即削除する） |
| `deploy/make-hash.html` | `config.json` 用のハッシュを作る手元用ページ（**サーバーには置かない**） |
| `legacy/` | Claude Chat時代の旧版（参照用・非稼働） |

## 重要なルール

- **フレームワーク・ビルドツール・外部CDN・npmパッケージは導入しない。** バニラJS（ES5相当）で書く。
- **利用者が入力する `<input type="text">` には `autocomplete="off"` を付ける**（店舗名・住所など）。
  付けないと、ブラウザの自動入力（Chromeのアドレス保存機能など）が意図しない値
  （例: 会社名などの保存済み候補）を初期値のように差し込むことがある（実際に報告された不具合）。
- **画面に表示する画像は data URI で `index.html` に埋め込む**（ロゴ・機器写真・スプラッシュのアイコンなど）。
  ただし**PWAに必要なファイル（`manifest.json` `sw.js` `icon-*.png`）だけは実ファイルにする**。
  Androidでは data URI のマニフェストだとWebAPKが作られず、Chromeのマークが付いたただのショートカットに
  なってしまうため。**この3つをdata URI化・削除しないこと。**
- **入力途中の内容は端末に一時保存する（下書き）。** スマホでは画面の更新やアプリの切り替えだけで
  入力が消えてしまうため、当日タブ（業態・店舗名・日付・時間帯・連絡文・機器台数）とカレンダータブの
  ①②③の入力を `localStorage`（キー: `oes-calendar-draft-v1`）へ400ms遅延で保存し、開き直したときに戻す
  （`saveDraftSoon()` / `restoreDraft()`）。**24時間経った下書きは使わずに消す**（前日の入力が残らないように）。
  復元は**共有設定の読み込みが終わってから**行う（先に戻すと行の作り直しで消えるため）。
  これは「入力中の内容」だけで、**登録リスト（`entries`）は従来どおり保存しない**。
- **設定の保存場所は「サーバー上の `settings.json` 1か所」**。起動時に `fetch` で読み込む（`SHARED_FILE`）。
  `localStorage`（キー: `oes-calendar-settings-v1`）は共有設定を読めなかったときの控えに過ぎない。
  保存は `settings-save.php` があればそこへPOSTし、無ければ `settings.json` をダウンロードさせて
  FTPでアップロードしてもらう（`saveSharedSettings()`）。**登録リスト（作業予定）は保存しない**（揮発）。
- **「保存し忘れ」で共有されない事故を起こさないこと。** 設定を変えたら `markSharedDirty()` が
  `sharedState.dirty` を立て、設定タブに未共有バナー（`#dirty-bar`）を出し、離脱時に警告する。
  起動時の `probeSaveEndpoint()` で `settings-save.php` が本当に使えると分かっている場合は、
  変更の1.5秒後に**自動でサーバーへ保存する**（管理者が押し忘れても共有される）。
  この「変更＝未共有として見せる」「使えるなら自動保存する」動作を外さないこと。
- **原因を利用者が自分で特定できるようにする。** 設定タブの `#diag-card`（`runDiagnostics()`）で
  https / `settings.json` / `settings-save.php` / パスワード設定 / `manifest.json` / アイコン /
  Service Worker / **手順書・資料アップロードの上限（`post_max_size`/`upload_max_filesize`）** /
  インストール状態を1つずつ確認し、✅⚠️❌と「どうすればよいか」を日本語で出す。
  アップロード上限のチェックは `manual-upload.php` を**GETで**呼ぶと診断用のJSON
  （`post_max_size`・`upload_max_filesize`・`sapi`・`max_bytes`）を返す仕組みを利用している
  （POST専用エンドポイントだが、GETのときだけ例外的にこの診断情報を返す。実際のアップロード動作
  ＝POST側の処理には影響しない）。アプリの上限（`MANUAL_MAX_BYTES`）より小さい場合は、
  `.user.ini`（PHP-FPM向け）または `.htaccess`（mod_php向け。`deploy/.htaccess-upload-limits.sample`）
  で引き上げるよう案内する。**利用者から「アップロードに失敗する」「上限を上げたい」という
  問い合わせが繰り返しあったため追加した機能。今後この種の問い合わせが来たら、まずこの
  診断結果を見てもらうこと。**
  **ロック中でも実行できるようにする**（管理者以外のスマホから確認する用途があるため、
  `#lock-card` `#install-card` と同様にロック対象から除外する）。
  `settings.json` の読み込み成功時は、業態の並び順とサーバー上の更新日時を `#shared-status` に出して、
  PCとスマホで見比べられるようにする。
- **設定タブは管理者パスワードでロックする。** ロック中は `#pane-set` に `locked` クラスが付き、
  CSSで入力・ボタンを操作不可にする。ただし**閲覧・折りたたみの開閉・「アプリとしてインストール」
  （`#install-card`）・「共有設定を再読み込み」（`#lock-card`）はロック中でも使える**
  （インストールや閲覧は管理者以外も必要なため、ロック対象から除外する）。
  変更系の関数には `requireAdmin()` を入れて二重に防ぐ。
  - **ロックのボタン名は「保存してロック」**（`lockSettings()`、以前は「ロックする」だった）。
    **押すと必ず先に`saveSharedSettings()`を呼んで共有設定を保存してから、ロック状態に戻す**
    （利用者の指定。「変更したのに保存を忘れてロックしてしまう」事故を防ぐため、名前どおり
    保存とロックを1回の操作にした。`saveSharedSettings()`は`adminPw`が消える前に呼ぶ必要があるので、
    `lockSettings()`内では**保存 → `adminPw`のクリア → ロック状態反映**の順を守ること）。
  - **同じ「保存してロック」ボタンを2箇所に置く**（`#btn-lock`＝`#lock-card`の上部、
    `#btn-lock-bottom`＝「共有設定の保存」カードの下部。利用者の指定：「このボタンは下部にも
    配置してほしい」。設定タブは長いので、上まで戻らずに一番下でも保存してロックできるようにする
    ため。どちらも`applyLockState()`で解除中だけ表示し、同じ`lockSettings()`を呼ぶ。
    ロジックの分岐や別実装を作らないこと）。
- 日本語UI。**配色は「濃い灰色（メイン `#33383D`）× 濃い赤（アクセント `#86100C`）」の2色（バイカラー）を維持する。**
  **色の変遷（新しく変える前に必ず把握すること。以前の色には絶対に戻さない）**:
  1. 最初は「青×空色」。
  2. 利用者の指定で「濃紺（`#003DA5`）×オレンジ（`#F2600D`）」に変更。
  3. 利用者の指定でオレンジを少し暗め（`#E25908`）に変更。
  4. **利用者が添付した「血圧警告」風の画像を参考に、現在の「濃い灰色（`#33383D`）×濃い赤（`#86100C`）」に変更**
     （`--blue`系統の役割はそのまま「濃い灰色」に、`--sky`系統の役割はそのまま「濃い赤」に置き換えている。
     変数名`--blue`/`--sky`は歴史的な名残で、現在の意味は「メイン＝濃い灰色」「アクセント＝濃い赤」）。
  ヘッダー・選択中タブ・カード見出しの番号丸はこの2色、カードの見出し帯・
  ボタンの薄色部分などの淡色背景はアクセント（濃い赤）寄りの薄いピンク系トーン（`--blue-l` `--blue-l2` `--line` `--line-w`）
  にする。危険操作（削除）の赤・自動生成タグの緑・手動編集タグの琥珀色などの意味付き色はこの2色ルールの
  対象外（そのまま維持してよい。**ただし危険操作の赤とアクセントの濃い赤は別の色**なので混同しないこと）。
  ロゴ由来のウェルシス株式会社バッジ（`.welsys-badge`）も対象外（実在の会社ロゴの色をそのまま使う）。
  実際の値は `index.html` の `:root` を参照（`--blue` `--blue-d` `--sky` `--sky-d` `--sky-l`
  `--blue-l` `--blue-l2` `--line` `--line-w` `--bg` `--ink` `--muted`）。
  **配色を変える指示が来たら、`:root`の変数だけでなく、ハードコードされた色（`.header-sub`・
  `.field > label`・`.btn-ghost`・`.pb-edit`・`#toast`・`.il-note`・スプラッシュの背景ウォッシュ・
  `.slot-edit`や`.ph`等の枠線・淡色背景・`manifest.json`の`theme_color`・PWAアイコン・
  `manual.html`の`:root`）も必ず一緒に洗い出して更新すること**（過去に何度もこの洗い出し漏れが
  発生している）。
  **2色は滑らかなグラデーションでぼかさず、はっきり分かれた面で表現する**
  （利用者の指定。以前は`linear-gradient(135deg,var(--blue),var(--sky))`のような2点グラデーションで
  自然にぼかしていたが、これは禁止。同じ色を2つ隣接ストップに置いて境界を作る
  `linear-gradient(135deg,var(--blue) 0%,var(--blue) 62%,var(--sky) 62%,var(--sky) 100%)` の書き方で、
  斜めにくっきり分かれた2色の面にする。`.header` `.tab.active` と `manual.html` の `h1` 背景がこの書き方。
  CSSの仕様上プロパティ名は`gradient`のままだが、見た目はぼかしのない硬い境界になる）。
  **ただし文字そのものに`background-clip:text`でこのグラデーションをかけるのは禁止**
  （スプラッシュのアプリ名で試したところ、文字の途中で色が切り替わって読みにくいと利用者から指摘済み。
  `#splash .sp-name` は単色（`var(--blue)`）に固定する）。
- **パソコン・スマートフォン両対応。スマホでの利用を重視し、余白・文字は必要最小限に詰める。**
  入力欄の `font-size` は16px以上を維持（iOSの自動ズーム防止）。タップ領域は**34px以上を死守**しつつ、
  それ以上は無闇に広げない（旧仕様の42px基準は廃止済み。詰めすぎて34px未満にしないことだけ守る）。
- **フッターのコピーライトのすぐ上に「アプリインストールはこちらから」を常時置く**
  （`.install-link` → `openInstallHelp()`）。`beforeinstallprompt` を受け取っていればそのまま
  インストールダイアログを出し、そうでなければ端末に合わせた手順（iOS Safari / Android Chrome / PC）を
  モーダルで案内する。**この導線を消さないこと。**
  **見た目は控えめな小さいテキストリンクにする**（絵文字・色付きの大きなボタンにはしない。
  タップ領域だけ34px以上を確保する）。目立たせすぎないことが利用者からの明示的な指定。
- **フッターのコピーライトは、利用者から支給されたデザインに合わせた「バッジ」で表示する**
  （`.welsys-badge`：淡い緑 `#E7F4ED` の角丸ピル＋白地のWELSYSロゴ＋濃緑 `#003416` 太字の
  `© ウェルシス株式会社`）。**西暦は入れない。** この見た目は指定されたものなので勝手に変えない。
- **起動時にスプラッシュ（`#splash`）を出す。** OESアイコン＋アプリ名「OES入替作業APP」＋
  フッターと同じWELSYSバッジを1.2秒表示してフェードで消し、DOMから取り除く。
  **`pointer-events:none` を維持すること**（操作を妨げず、自動テストのクリックも遮らないため）。
- **カレンダーの月グリッドは廃止した。** 稼働件数ぶんの行にそれぞれ日付入力（`<input type=date>`）が付くため、
  月をめくって視認するUIは不要になった。カレンダーグリッド（`.cal-*`／`.day`／`renderCalendars()`等）を復活させないこと。
- **カレンダー画面は「次に何をすればよいか」を常に示す。** 各カード（`.step-card` / `#step1`〜`#step3`）の見出しに
  進捗バッジ（`#badge1`〜`#badge3`。`0 / 3件` のような達成数）を出し、いま取り組むカードに `now` クラスで枠を付ける。
  その項目が終わったら次のカードへ進む案内（`.step-next` / `#next1`〜`#next2`）を出し、
  「次へ」ボタンで次のカードへスクロールして最初の入力欄にフォーカスする（`updateWizardNav()` / `goStep()`）。
  入力のたびに `updateWizardNav()` を呼ぶこと。
- **「カレンダー」画面は質問形式（ウィザード）で、①業態と稼働件数 → ②日付・時刻・店舗名・住所をまとめて入力 →
  ③一覧で確認してGoogleカレンダーへ登録、の3ステップで1画面に完結する。**
  **かつては②日付と時刻→③店舗名と住所を別ステップに分けていたが、「1件目で日付も店舗名、住所も
  入力できた方が入力しやすい。日付時間だけ先にして、あとで店舗名だとごちゃごちゃになる」という
  利用者の指摘により、1件ごとに日付・時刻・店舗名・住所をまとめて入力する1ステップへ統合した
  （旧④の登録リストは③に繰り上げ）。この統合を分割方式に戻さないこと。**
  - ①「業態と稼働件数を選ぶ」: `#w-gyotai`（業態）・`#w-count`（稼働件数、1〜30件から選ぶ）のみ。
    **担当者に関するものは一切置かない**（担当者の概念そのものを廃止済み）。
    選んだ業態の午前・午後それぞれの時刻を`#w-time-hint`に表示するだけの参考情報とする。
  - ②「日付・店舗名を入力」: `#item-rows`に稼働件数ぶんの行（`.item-row`）が並び、各行が1件分の
    **日付（`<input type=date>`）、開始・終了の時刻プルダウン（`.dr-start` / `.dr-end`）、
    店舗名（`.sr-tenpo`）、住所（任意・`.sr-addr`）を1か所にまとめて持つ**（`itemRowHtml(i)`）。
    同じ日を複数件でも、別々の日でもよい。**午前/午後のトグルは廃止した。時刻を直接指定する方式に戻さないこと。**
    業態を変えたときは各行の時刻をその業態の最初の時間帯に入れ替える（`applyGyotaiTimesToRows()`）。
    終了が開始より前になったら自動で揃える（`fixRowTimes()`）。
    どの定型文を使うかは**開始時刻に一番近い時間帯**から決める（`slotNearestTime()`）。
    **1件目の開始・終了時刻を変更すると、2件目以降にも同じ時刻をそのままコピーする**
    （`applyFirstRowTimeToOthers()`。毎回同じ時刻を入力し直す手間を省くための利用者からの指定）。
    件数を増やして新しく行が増える場合も、1件目が入力済みならその時刻を初期値にする
    （`firstRowTimesOrDefault()`。1件目が未入力なら従来どおり業態の既定時刻を使う）。
    2件目以降は個別に変更してもよいが、1件目を再度変更すると全行にまた上書きされる。
    「このN件を登録リストへ」（`submitStoreRows()`）で、**日付が入っている行だけ**を登録リストに追加する
    （日付未選択の行は黙ってスキップし、件数はトーストで知らせる）。店舗名・住所は空欄でも登録してよい。
    業態は①で選んだものを全行共通で使う。登録後は入力を空に戻す（`resetStoreRows()`）。
  - ③「一覧で確認してGoogleカレンダーへ登録」: 登録リストは既定で折りたたみ表示（`.entry`に`open`クラスが無ければ
    本文非表示）にして、件数が多くても一覧が長くなりすぎないようにする（`toggleEntry(id)`で開閉、見出し行に要約と
    「登録」ボタンを常時表示）。カードを開いたときの編集項目は日付・業態・時間帯・開始・終了・店舗名・住所・説明文で、
    担当者に関する欄は無い。「Googleカレンダーで順番に登録」（`startQueue()`）と「ICSファイルで一括取込」
    （`downloadIcs()`）の両方をここに置く。
- **設定の「よく使う文」の編集欄は1行の `<input>` にして、操作ボタンと横並びにする**（`.ph-row`）。
  縦に長くならないようにするための指定なので、複数行のtextareaに戻さないこと。
- **作業当日タブでも時刻を直接指定する**（`#day-start` / `#day-end`）。業態を選ぶとその業態の最初の時間帯の時刻が
  初期値として入り（`applyGyotaiTimesToDay()`）、そこから自由に変えられる。どの定型文を使うかは開始時刻から決める。
  **時間帯（午前/午後）のラジオは廃止した。**
- **作業当日タブに「クリア（最初からやり直す）」を置く**（`clearDayAll()`）。店舗名・連絡文・機器台数・
  入店連絡と中間報告の選択・貼り付け欄・下書きをすべて消して初期状態に戻す。
- **カレンダー画面①のカードにも「クリア（最初からやり直す）」を置く**（`clearWizardAll()`、`#btn-cal-clear`）。
  ①業態を先頭の項目へ・稼働件数を1件へ戻し、②の入力（日付・時刻・店舗名・住所）を空に戻す。
  **③の登録リスト（`entries`）もこのボタンで一緒に消す**（利用者の指定：「最初からやり直す」だから
  まだGoogleカレンダーへ登録していない③の一覧も含めて全部消えてほしい、という要望で、
  当初は③を対象外にしていたが変更した）。**`entries.length` が1件以上あるときだけ`confirm()`で
  確認する**（意図しない一括削除を防ぐため。0件のときは確認なしでそのままクリアする）。
  既存の「全て削除」（`clearAllEntries()`、③の一覧内）とは別に、こちらからも消せるようになったが、
  「全て削除」自体は削除しない（③だけ消したい場合のために残す）。
  `clearDayAll()` と違い `clearDraft()` は呼ばない（呼ぶと当日タブ側の下書きまで消えてしまうため）。
  `renderStoreRows()` で行数を1件に揃えてから `resetStoreRows()` で値を空にする。
- **①入店連絡・②中間報告に足す文は選択式にする。** `settings.entryNotes` / `settings.midNotes`
  （どちらも `[{id, text}]`）の文をそれぞれの欄の上にチェックボックス（`#entry-note-row` / `#mid-note-row`、
  複数選択可）で並べ、**チェックした文だけ**をその本文の1行目（見出し）の直後に設定の並び順で差し込む
  （`rebuildEntryNoteBlock()` / `rebuildMidNoteBlock()`）。機器台数と同じ「選んだものだけ入れる」方式。
  - `entryNotes` 初期値: POSレジ清算済み／POSレジ清算 今から開始をお願いいたしました
  - `midNotes` 初期値: 高所の為、電源ケーブル流用させて頂きます／スパイラル多用の為、電源ケーブル流用させて頂きます／
    店舗名多忙の為、入替作業は状況見て実施します。
  **後者3文を入店連絡（entryNotes）側に戻さないこと。中間報告用として利用者から明示的に指定されている。**
  **これらを業態の定型文に直接書き戻さないこと**（`TPL_SUKIYA` からPOSの2行は削除済み）。
  設定画面「入店連絡に足す文」「中間報告に足す文」（2枚の別カード）で、それぞれ追加・編集・並び替え・削除ができる。
- **③退店連絡の本文欄（`#msg-out`）の下に「特記事項」の入力欄（`#msg-remark`）を置き、
  「コピー」ボタンは特記事項のさらに下＝③の一番最後に置く**（利用者の指定。かつては特記事項が
  コピーボタンの下にあったが、「先に特記事項まで入力してから、まとめてコピーする」流れに合わせて
  順番を入れ替えた。③だけ他の欄と並びが違う理由でもある）。
  `#msg-remark`は`textarea`の既定`min-height:130px`だと縦に長くなりすぎるため、
  **`#msg-remark{min-height:68px;}`で2〜3行程度の高さに個別に上書きしている**
  （利用者の指定。他のtextareaの既定の高さは変えないこと。`resize:vertical`なので手動で広げることは可能）。
  入力された場合だけ、退店連絡の本文の**いちばん最後**（機器台数よりさらに後ろ）に、
  **`【特記事項】`という見出し付きで**空行をはさんで追記する（`REMARK_HEADING`）。
  空欄・空白だけのときは見出しごと何も足さない。
  本文は `rebuildOutBlock()` で「本文 → 機器台数 → 【特記事項】＋入力内容」の順に毎回組み立て直す
  （`dayRemark` は入力内容そのもの、`appliedRemark` は直前に本文へ入れた見出し込みのブロック）。
  書き換えても二重にならないよう、`appliedRemark` を目印に取り除いてから足し直す。
- **作業当日タブの③退店連絡にある機器台数（MPR/MST/HT/BC/BP）は、入力した機器だけ反映する。**
  台数は**1〜10のプルダウン（`<select>`）で選ぶ**（数値入力の`<input type=number>`には戻さない）。
  空欄（未選択）のものは表示しない（テンプレート由来の空欄プレースホルダ行も読み込み時に除去する）。
  この「入力したものだけ出す」動作を、初期状態で全項目を表示するような実装に戻さないこと。
- **作業当日タブの①②③の各テキスト欄には「コピー」ボタンだけを置く（欄の直下、ラベルの隣ではない）。**
  かつて欄ごとに「Chat」ボタンも置いていたが、Google Chat側のCOOP制約により「開き直さず前面に
  出すだけ」を確実に行う方法が無く、利用者から繰り返し不具合報告があったため**①②③個別のChatボタンは
  廃止した**（利用者の指示。復活させないこと）。「コピー」ボタンは①②では欄の入力内容のすぐ下＝
  その欄の最後の要素として配置する（3つの欄を続けて縦に並べても誤タップしないよう、ボタンの後に
  しっかり余白＝`.field.msg-field`の`margin-bottom:20px`を取ること）。
  **③退店連絡だけは例外で、コピーボタンは本文欄の直下ではなく「特記事項」欄のさらに下に置く**
  （前項参照。特記事項まで入力してからまとめてコピーする流れのため）。
- **Google Chatを開く入り口は、②「今日の作業」カードの「Google Chat を開く」（`#btn-open-chat`）と、
  画面下部「よく使う文」欄の直前に置いたもう1つのボタンの、合計2箇所にする**
  （利用者の指定。よく使う文まで下にスクロールしたときに、わざわざ上の②のカードまで戻らずに
  Google Chatを開けるようにするための追加。**呼び出す関数（`openStoreChat()`）は完全に同じもの**で、
  ロジックの分岐や別の実装を作らないこと。`dayCtx`が未設定＝連絡文をまだ作っていない状態で押しても
  クラッシュしない設計になっている（`openStoreChat()`内で`dayCtx ? … : ''`のように空文字にフォール
  バックする）ので、下部のボタンは特に条件分岐なしでそのまま同じ関数を呼べばよい）。
  業態・店舗ごとのURL事前登録（`settings.storeChats`）は**廃止したまま**
  （利用者の指示。データ構造ごと削除済み。復活させないこと。下記の理由は変わらない）。理由:
  1. 登録の手間がかかる。
  2. 登録したURLがルーム/一覧のトップだと、Chat内で他のスレッドへ移動したあとにもう一度
     ボタンを押すと元のURLへ戻ってしまう（COOPの制約で回避不能。過去に3方式を試し、
     いずれも実機か自動テストで問題が確認された。詳しい経緯は`openStoreChat()`の
     コード内コメントに残してある）。
  3. **Google Chatの検索欄はURLと連動しない**（クライアント内で完結する検索で、検索してもアドレス
     バーのURLは変化しない。利用者に実機で確認済み）ため、URLで検索語を事前入力する方法も無い。
  そのため`openStoreChat()`は、**毎回`window.open('https://chat.google.com/', 'oes-store-chat')`で
  一般のGoogle Chatを名前つきウィンドウで開くだけ**にする（同じ名前のウィンドウがすでに開いていれば
  ブラウザ側で再読み込みは起きない）。ポップアップ側の状態には一切関与しない
  （`.closed`・`.focus()`成否・`document.hasFocus()`はいずれも判定に使わない。理由は上記の通り不可能なため）。
  **以前は「業態 店舗名」（`dayCtx.gyotai + ' ' + dayCtx.tenpo`）をクリップボードへコピーしていたが、
  廃止した**（利用者の指示。①入店連絡／②中間報告／③退店連絡の「コピー」ボタンで直前にコピーした
  内容を、Chatを開くだけのつもりで上書きしてしまう恐れがあるため。**復活させないこと**。
  クリップボードへの自動貼り付けができない事情（別オリジンのページをJSから操作できない、
  Google Chatの検索欄はURLと連動しないクライアント内完結の検索であることを利用者に実機で確認済み）
  は、そもそもURLに検索語を含める方式を採らなかった理由として上記に残してある）。
- **PWA対応（ホーム画面に追加できる）。** Androidで**Chromeのマークが付かない独立したアプリ（WebAPK）**として
  登録されるには、次がすべて必要（1つでも欠けるとただのショートカットになる）。
  ①`https://` 配信 ②`<link rel="manifest" href="manifest.json">`（**実ファイル**。`.webmanifest` は
  未知の拡張子として扱うサーバーがあるため **`.json` にする**）
  ③`manifest.json` に `id` `start_url` `scope` `display:standalone` と
  192/512/512-maskable のアイコンを**実URL**で書く ④`sw.js`（fetchハンドラを持つService Worker）を登録する。
  **PWAアイコンは `index.html` と同じ階層に置く**（`icon-192.png` `icon-512.png` `icon-512-maskable.png`）。
  `assets/` の中だけに置くと、`assets/` をアップロードし忘れた環境でアイコンを取得できず、
  アイコンの無いショートカットになってしまうため。`assets/` 側の同名ファイルは manual.html 用に残す。
  `sw.js` はネットワーク優先（FTPで差し替えたらすぐ反映されるように）で、
  `settings.json` `config.json` `*.php` はキャッシュしない。アイコンは
  `assets/device-printer.jpg`（プリンター写真そのものは変更しない）に**バッジ（現在は濃い灰色
  `#33383D`。バイカラーの節を参照。変更したらここも合わせて変える）**＋白太字の「OES」を
  重ねたもの（`icon-*.png` に元データを同梱）。
  **バッジの文字はオレンジではなく白にする**（オレンジ文字は濃色の上で見づらいと利用者から指摘済み。
  白地にプリンターの写真＋バッジ＋白文字、という構成を維持すること。文字をオレンジに戻さない）。
  アイコンやアプリ名を変える場合は `manifest.json` と `icon-*.png` を差し替える
  （favicon・apple-touch-icon・スプラッシュ用のアイコンはいずれも index.html 内の data URI なので、
  Pythonなどで新しい `icon-*.png` から base64 を作り直して該当箇所を差し替える）。
  設定タブに「📱 アプリとしてインストール」カードを置き、`beforeinstallprompt`イベントを受け取れた場合だけ
  ボタン（`#btn-install-pwa`）を表示してその場でインストールできるようにする（`installPwa()`）。
  イベントに対応しないブラウザ（iOS Safari等）ではボタンは出さず、下の案内文とマニュアルへのリンクのみ表示する。
- **業態は設定画面で並び替えできる**（各業態の見出しの「↑」「↓」＝`moveGyotai()`）。
  この並び順がそのままカレンダー画面の業態プルダウンの順番になる。
- **業態ごとに「注意事項」（`g.notes`、`[{id, place, text}]`の配列）を複数登録できる。**
  `place`は`'in'`（①入店連絡）／`'mid'`（②中間報告）／`'out'`（③退店連絡）のいずれか。
  設定画面の各業態の中に「＋ 注意事項を追加」（`addGyNote()`）があり、1件ごとに
  「表示する欄」の選択（`setGyNotePlace()`）と自由記述のテキストエリア（`setGyNoteText()`）、
  削除ボタン（`removeGyNote()`）を持つ（**利用者の指定で、単一の自由記述欄から
  「配置を選べる・複数登録できる」形に変更済み**）。報告用の写真の撮り方・撮影項目などを想定。
  **空欄（該当する`place`の注意事項が無い）欄には何も表示しない。**
  作業当日タブの③カード内、①②③それぞれの連絡文欄（`#msg-in`/`#msg-mid`/`#msg-out`）の直前に
  同じ`place`の注意事項だけをまとめて表示する（`#day-gyotai-notes-in`/`-mid`/`-out`、
  `updateDayGyotaiNotes()`）。同じ欄に複数あれば空行区切りで並べる。業態を切り替えるたびに更新し、
  該当が無い欄は`hidden`属性でごと消す。
  **旧仕様（1業態1本の自由記述文字列）からの互換**: `normalizeSettings()`の`normalizeGyotaiNotes()`が、
  文字列のままの`g.notes`を「①入店連絡向けの1件」として自動で配列に変換する（過去の設定が消えないように）。
  **各行の先頭・末尾の空白は`trimLines()`で取り除く**（textareaの入力ぶれで見た目がずれるのを防ぐため。
  利用者から実際に「見え方がずれている」と指摘されて追加した処理）。
  **HTMLテンプレート側の注意**: `.day-notes`は`white-space:pre-wrap`なので、
  `<span class="dn-label">`と本文の`<span>`の間に改行やインデント用の空白を入れると、
  その空白がそのまま描画されて1行目だけ字下げされたように見える不具合になる
  （実際に発生した不具合。タグの間には空白を入れず詰めて書くこと）。
- **業態ごとに「手順書・資料」（画像・PDF）を複数登録できる（`g.manuals`、`[{id, name, type, url, size}]`の配列）。**
  作業当日タブで業態を選んだ瞬間に、探すことなくそのまま開ける参照資料（手順書・注意事項の写真など）を想定した機能。
  **アップロード・削除ができるのは管理者だけ**（`requireAdmin()`で二重にガードする）。
  - 実体のファイルはブラウザ内やsettings.jsonには置かず、**サーバー上の`manuals/`フォルダ**に保存する
    （`manual-upload.php`が保存し、`settings.json`には`{id, name, type, url, size}`という参照だけを持つ。
    settings.jsonを肥大化させないため。画像を`data:`URIやbase64で`settings.json`に入れる方式にしないこと）。
  - 設定画面の各業態の中に「＋ ファイルを追加（画像・PDF）」（`addGyManualClick()`→隠しファイル入力
    `#gyfile-<業態id>`→`onGyManualFileChange()`）があり、選ぶと`uploadGyManual()`が管理者パスワード付きで
    `manual-upload.php`へ`FormData`でPOSTする。返ってきた`url`等を`g.manuals`に追加し、通常の設定変更と
    同様に`afterSettingsChange(true)`で共有設定へ反映（自動保存）する。
  - 削除は`removeGyManual()`。`confirm()`で確認後、`g.manuals`から即座に取り除いて共有設定を保存しつつ、
    サーバー上の実ファイルの削除は`manual-delete.php`へ**うまくいかなくても気にしない形（fire-and-forget）**
    で依頼する（一覧から消えることが利用者にとっての「削除完了」であり、孤立ファイルが1つ残る程度は実害がないため）。
  - 対応形式は画像（jpg/png/gif/webp）とPDF、**1件30MBまで**（`MANUAL_MAX_BYTES`。サーバー側`manual-upload.php`の
    上限と必ず合わせること）。クライアント側の`accept`属性やMIMEチェックは利便性のためのものに過ぎず、
    **本当の検証はサーバー側（`finfo`による実体判定）で行う**（クライアントが偽装したMIMEやファイル名は信用しない）。
  - `manual-upload.php`は保存先の`manuals/`フォルダを初回アクセス時に自動作成し、その中に
    実行不可化のための`.htaccess`（`php_flag engine off`など）も自動で書き込む
    （多層防御。アップロードされたファイルの中で万一スクリプトが実行されないようにするため）。
    保存ファイル名は元のファイル名を使わず`bin2hex(random_bytes(10))`のランダム名にする
    （パス操作・上書き事故防止。元のファイル名は表示用の`name`としてのみ`settings.json`に残す）。
  - `manual-delete.php`は`url`をそのまま信用せず`basename()`でファイル名部分だけを取り出してから
    `manuals/`フォルダ内のパスを組み立てる（`../`等でのパス操作を防ぐため）。
  - 設定画面の一覧（`.gyman-name`）・作業当日タブの一覧（`.dm-name`）は**どちらもファイル名を
    省略せず全文表示し、長ければ折り返す**（`white-space:normal;word-break:break-all;`。
    以前は両方とも`text-overflow:ellipsis`／`.dm-name`は`max-width:180px`で1行に切り詰めて
    いたが、どのファイルか分からなくなるという利用者の指摘で両方とも変更した。日本語ファイル名も
    `word-break:break-all`で単語区切りに関係なく折り返す）。
  - 作業当日タブでは、**「この内容で連絡文を作る」ボタンの下、「クリア（最初からやり直す）」ボタンの上**
    に一覧を表示する（`#day-gyotai-manuals`／`updateDayGyotaiManuals()`。利用者の指定による配置。
    当初は業態プルダウンの直下に置いていたが、この位置に変更した）。
    **表示の有無・中身は連絡文を作ったかどうかに関係なく、業態を選んだ時点で決まる**
    （`onDayGyotaiChange()`から呼ぶ。DOM上の位置を移しただけで、この条件は変えていない）。
    画像はサムネイル、PDFは「PDF」バッジで表示する。
  - **タップ（クリック）したときのリンク先は`manualHref(m)`で種類ごとに変える。**
    画像はそのまま`m.url`（`manuals/xxx.png`）を直接開く。**PDFは`manual-view.php`を経由させる**
    （`manual-view.php?file=<manuals/内のファイル名>&name=<元のファイル名>`）。理由:
    ホーム画面から起動したPWA（WebAPK）の中でPDFをその場で開こうとすると、Androidの仕様で
    同一オリジンのページがアプリ内に留まってしまい、PDFがまともに開けない
    （`window.open()`で別ウィンドウを試みても改善しないと利用者から報告された）。
    `manual-view.php`はファイルを`Content-Disposition: attachment`で返す＝ダウンロードとして渡すことで、
    Android側の「ダウンロード完了→開く」の流れに乗せ、Acrobat Readerなど端末の既定のPDFアプリで
    開けるようにする（`filename*=UTF-8''...`で日本語ファイル名も正しく渡す）。
    画像はどのブラウザでも問題なく表示できアプリへの受け渡しが不要なため、この対象外（従来どおり直接開く）。
    リンクのクリック自体は引き続き`openManualWindow()`（`window.open()`を明示的に呼ぶ）を通す
    （ポップアップブロック時は`<a target="_blank">`本来の遷移にフォールバック。PDF側は最終的に
    ダウンロードとして処理されるため実害はない）。
    該当する資料が無い業態を選んだときは`hidden`属性で非表示にする。
  - **PHPが使えないサーバーでは利用できない**（`settings-save.php`と異なり、ファイルの保存には
    サーバー側の処理が必須で、ダウンロード＋FTPアップロードのような代替経路は用意していない）。
    `manual-upload.php`が無い/使えない場合は、アップロード時にトーストで
    「サーバーに manual-upload.php がありません（管理者にご確認ください）」等、原因が分かるメッセージを出す
    （`uploadGyManual()`のエラーハンドリング）。
  - **実際に発生した不具合と対策**: 共用サーバー（特にPHP-FPM）では`post_max_size`が
    アップロードしたいファイルより小さいことがある。この上限を超えると、**PHPはエラーにせず
    `$_POST`と`$_FILES`を丸ごと空にする**ため、パスワードが正しくても`manual-upload.php`側では
    「パスワードが違います」という**見当違いのエラー**になってしまう（実際に利用者から報告された不具合）。
    これを防ぐため、`manual-upload.php`はパスワード照合より**先に**
    `$_SERVER['CONTENT_LENGTH']`と`ini_get('post_max_size')`を比較し、超過が疑われる場合は
    「ファイルが大きすぎてサーバーの上限（post_max_size = …）を超えています」という具体的なメッセージを返す。
    同様に`upload_max_filesize`超過（`$_FILES['file']['error'] === UPLOAD_ERR_INI_SIZE`）も
    専用のメッセージにする。**この2つの上限チェックをパスワード照合より後ろに動かさないこと**
    （後ろにすると、上限超過時は`$_POST`が空＝パスワードも空になっているため、絶対に先にたどり着けない）。
    あわせて `deploy/.user.ini.sample`（`.user.ini`にリネームしてindex.htmlと同じ場所に置くと、
    PHP-FPM環境でこのフォルダだけ`post_max_size`/`upload_max_filesize`を引き上げられる）を用意した。
    **`.user.ini`はPHPがApacheモジュール（mod_php）として動くサーバーでは効かない**
    （PHP_INI_PERDIRの一部設定はCGI/FastCGI＝PHP-FPM経由でのみ`.user.ini`から読み込まれる仕様のため。
    利用者が実際に`.user.ini`を設置しても数値が変わらない不具合が報告され、原因はこれだった）。
    その場合の代替として `deploy/.htaccess-upload-limits.sample`（`php_value`をmod_php向けの
    `<IfModule>`で囲んだもの）を用意した。**どちらが効くかはサーバーによって違うため、
    両方試してもらう案内にすること**（`.user.ini`のほうが安全なので先に案内する）。
    設定タブの「サーバーの状態をチェック」（`runDiagnostics()`）で、実際に効いている
    `post_max_size`/`upload_max_filesize`の値を確認できるようにしてある（前項参照）。
- **「📋 コマンド一覧」タブは、実機（SEIKO SA-2610 マルチステーション）の操作パネルのシミュレーター。**
  「この保守機能はどのボタンをどの順番で押すのか」を、画面上の実機を操作しながら覚えられるようにするための機能
  （利用者の依頼で追加。当日タブと設定タブの間に置く）。
  - **カードのタイトルは「Monstera/Relier シミュレーター」**（利用者の指定。SA-2610/SA-3610という
    実機のモデル名とは別の名称だが、そのまま使うようにと明示的に指定された）。
  - **画面は1枚のカードにまとめ、「①」「②」のような番号付きの区切りは置かない**（以前は
    「①機器の画面（シミュレーター）」「②操作を選んで手順を見る」の2枚のカードに分けていたが、
    利用者の指示「この画面で、1、2と分ける項目は必要ない」で1枚に統合した。分割方式に戻さないこと）。
    カード内の並び順は、シミュレーター本体 → **直後**に「調べたい操作」のプルダウン（`#cmd-select`。
    利用者の指示「画面シミュレーターのすぐ下に調べたい操作のプルダウンを配置して」「ディスプレイの
    下には、調べたい操作を配置して」。`#sim-msg`/`#sim-note`/長押し等の案内文を挟まない） →
    役立つ場面・説明 → 再生ボタン・秒数設定・簡易手順（左）／進行状況（右）（この組み合わせは
    「まとめて表示できるようにして構わない」という利用者の言葉どおり、厳密な区切りを設けず
    `.cmd-guide-body`でひと続きに表示する。**「進行状況」には手順チップ（`#cmd-steps`）だけでなく、
    案内メッセージ（`#sim-msg`）・結果コメント（`#sim-note`）・長押し等の固定の案内文（`.hint`）も
    まとめて置く**――利用者の指示「進行状況と添付画像の文章表示して」で、以前シミュレーターの
    直下にあったこの3つを進行状況側へ移した）。
  - **手順は共有設定に入れる**（`settings.commands` = `[{id, name, note, steps:[{id, keys, hold, repeat, l1, l2, memo}]}]`）。
    管理者が設定タブで追加すれば全員に反映される。`keys`は`SIM_KEYS`のidの配列で、**2つ入っていれば同時押し**、
    `hold:true`なら**長押し**（`repeat`は自動的に1に戻る＝holdとrepeatは排他）、`repeat`(1〜20)は
    **同じボタンを連続で何回か押す**手順（旧フィールド`display`は`l1`の後方互換として読み込む）。
    `l1`/`l2`はそのボタンを押し終えたときに画面（LCD）の1行目・2行目へ出る文字（**上下行を別々に持つ**）。
    `memo`は**画面は変わらないが実際には何かが起こる操作**（明細票の印刷など）の説明で、LCDには出さず
    シミュレーターの案内エリア（`#sim-note`）にだけ表示される。
  - **`SIM_KEYS`のidは手順データに保存される値なので、あとから変えない**（変えると登録済みの手順が壊れる）。
    キー配列は実機どおりの7列×3行（`F △ シフト A1 B2 C3 電源` / `◁ メニュー ▷ D4 E5 F6 0` /
    `（黒） ▽ 実行 7 8 9 クリア`）。`BLANK`は押せない飾りのキー。
    **キーの色は実機の見た目を再現するための例外**（アプリ全体の2色ルールとは別）：
    F・△・シフト・◁・▷・▽・メニュー・電源・クリアは画面と同じ黒（`.k-black`）、実行だけは茶色（`.k-gold`）、
    それ以外（数字）は既定の明るい色のまま。
    **「シフト」「メニュー」は文字数が多く既定の大きさ（13.5px）だと折り返してしまうため、
    `.k-tight`（9.5px・字詰め・`white-space:nowrap`）で1行に収めている**（`.k-gold`の`実行`と
    同じ考え方。他のキーで折り返す場合もこのクラスを追加するか、専用のクラスを作る）。
    LCD（`.sim-lcd-box`）は実際の表示部だけで、`SEIKO`のブランド名やモデル名（`SA-2610`）は
    その外側の枠（`.sim-screen`の地色の上）に出す（「ディスプレイの中に見えてしまう」と分かりにくいため分離）。
    画面が広い（760px以上）ときにキーパッドが右へ来るレイアウトで、画面とキーの間に不要な余白が
    空かないよう`.sim-top`は内容ぶんの幅だけを取る（`flex:0 0 auto`。前は`flex:1 1 auto`で伸びて隙間ができていた）。
  - **画面・LED・枠の配色は、利用者が添付した実機写真（黒枠・うす緑がかった液晶・緑のLED表示）に
    合わせている。** 以前は薄い灰色の枠に白いLCD（黒文字）だったが、写真を見た利用者から
    「液晶：少し薄い緑がかった画面に黒い文字」「周りの枠：黒枠に『SEIKO』の白い文字、および
    『マルチステーション』『プリンター』などの文字が書いてある」という指定を受けて変更した。
    - `.sim-top`（LEDと画面をまとめる帯、実質的な「黒枠」）を黒（`#141414`、枠線`#000`）にし、
      LED（`.sim-led`）の文字とアイコンを緑（点灯時はさらに明るい緑＋グロー）にした。
    - `.sim-screen`は独自の背景を持たず（透明）、`.sim-top`の黒地の上にブランド名`SEIKO`・
      モデル名`SA-2610`/`マルチステーション`を**白文字**で表示する。
    - `.sim-lcd-box`（実際の表示部）だけを**うす緑がかった地色**（`#C7D5A0`）にし、文字は
      黒に近い濃緑（`#16240C`）のまま。**表示部の固定サイズ（幅211px・高さ64px。後述の
      サイズ調整で270px・54pxから変更）はこの配色変更自体では変えていない**
      （「サイズが変わるのは嫌」という以前からの指定を維持）。
    - **枠の余白（`.sim-top`のpadding/border）は左・上・下は最小限、右だけは黒い余白を
      広めに取ってある**（`padding:3px 16px 3px 3px`。利用者の指定「ディスプレイの右側の
      黒の余白が狭すぎる。黒の余白増やしてほしい」に対応。電源スイッチ（後述）と並べても
      固定幅211pxの表示部と衝突して横スクロールが出ないよう、右の余白ぶんは`.sim-screen`の
      幅を縮めて確保している（厚くすると`390px`幅のスマホで横スクロールが出る不具合が
      実際に発生したため、幅を削って帳尻を合わせる必要がある）。
  - **操作の入り口は2つあり、どちらも同じ手順データを使う。**
    ①プルダウンで操作を選ぶ → チップで「9×2 → 8 → 実行 → 完了」のように手順が出て、「順番に再生」で
    自動再生、「1つ進む」で1ステップずつ、実機のキーを自分で押しても進む（`cmdGo()` / `cmdPlay()`）。
    **「順番に再生」の間隔は`#cmd-play-speed`（②カードの「次に進むまでの秒数」）の既定値を
    1.5秒とする**（利用者の指定。0.8〜3秒の間で見る人がその場で変更できる）。
    `setCmdPlaySpeed(v)`が`cmdPlayStepSec`（通常間隔の秒数、共有設定には入れない）を書き換え、
    連続押し1回ぶん・押しまま・長押しの間隔は、比率計算の基準値である1200ms
    （＝通常1200ms・連続押し1回ぶん650ms・押しまま800ms・長押し2200ms。「動画（自動再生）が
    少し早い」という声を受けて、元の850/480/500/1600msから読む余裕を持たせて伸ばした値）との
    比率で一緒に伸び縮みする（`cmdPlayIntervals()`）。**この1200という基準値は既定の選択値
    ではなく計算の基準なので、既定の選択値（1.5秒）を変えてもここは変えないこと。**
    `cmdPlay()`は開始時に`cmdPlayIntervals()`を1回だけ呼ぶので、**再生の途中で速度を変えても
    その回には反映されない**（次に「順番に再生」を押したときから反映される。仕様どおり）。
    **シミュレーターの動き（実質的な「動画」）だけでなく、手順を上から順番に読める文章のリストも
    出す**（`#cmd-textlist` / `renderCmdTextList()`。`.cmd-guide-body`で左右2列にし、
    左＝再生ボタン・秒数設定・簡易手順のテキスト、右＝進行状況（手順チップ＋案内文）。
    画面が狭いときは縦に並ぶ）。
    `renderCmdSteps()`の最後で必ず一緒に呼ばれ、いま案内中の行を同じタイミングでハイライトする。
    ②プルダウンを使わず自由にキーを押す → 押した履歴（`simInput`）と登録済みコマンドを
    「1回押し単位」に展開したもの（`expandPhysicalSteps()`、`repeat`を実際の押す回数ぶんに開く）とを
    照合し（`simMatchProgress()`）、**途中まで一致していれば途中の画面、最後まで一致すればその操作の画面**を出す。
    ②のおかげで「電源長押し→確認表示→もう一度押して終了」のような段階的な操作も再現できる
    （途中の表示を出さないと、長押ししても何も起きないように見えるという不具合があったため、
    **前方一致での途中表示は消さないこと**）。**画面に出す文字（l1/l2）が1つも登録されていない操作は、
    完了時も実機と同じく画面を変えない**（`simShowCommand()`でコマンド名をLCDに出すフォールバックは廃止済み。
    そのぶん`memo`があれば案内エリアに表示する）。
  - **長押し・同時押しはpointerイベントで判定する**（`simDownKey()` / `simUpKey()`）。
    押されているキーが全部離れた時点で1回の操作として確定し、押していた時間が`SIM_HOLD_MS`(600ms)以上なら長押し、
    その間に押されていたキーをまとめて「同時押し」として扱う。**マウスでは同時押しできない**
    （ポインタが1つしかないため）ので、同時押しの確認はスマホの実機か、自動テストの
    `PointerEvent`を2つ送る方法で行う。
  - **ハイライトは押し方で色分けする**（`.simkey.on`＝赤、`.simkey.on.hold-mode`＝長押しは琥珀色、
    `.simkey.on.rep-mode`＝連続押しは蛍光ブルー`#0066FF`、連続押し中はキーの上に`.rep-badge`で
    「今何回目/全部で何回」を表示、`.simkey.on.stay-mode`＝押しままは蛍光グリーン）。
    同じ色分けを手順チップ（`.cmd-step`）にも適用している。
    **`rep-mode`にアプリ共通の`--blue`は使わない**（実際は`#33383D`という濃いグレーで、名前に反して
    青く見えず目立たないと利用者から指摘されたため、専用の鮮やかな青をハードコードしている）。
    **どのハイライトも静止した色ではなく常時アニメーションさせている**（「光っているように見せたい」
    という要望のため）。通常・長押し・押しままは`box-shadow`が呼吸するように強弱する
    グロー（`simglow-red`/`simglow-amber`/`simglow-green`）、**連続で何回か押す手順（`rep-mode`）だけは
    `steps(1,end)`で滑らかにせず、はっきり色が切り替わる点滅**（`simblink-blue`。「同じキーを2回押す
    ときもちゃんと点滅させてほしい」という要望のため、グローと差別化した）。
    自由に押しているとき（ガイド未選択）に実際に600ms以上押さえている状態は`.simkey.hold`単体で
    `simglow-red`を使う（`.on.hold-mode`/`.on.stay-mode`のほうがセレクタが詳しいのでガイド中は
    そちらが優先される）。
  - **待機中（何も操作していない）の画面は複数登録でき、プルダウンで切り替えられる**
    （`settings.idleScreens = [{id, name, l1, l2}]`。実機は電源の状態や二重化の構成によって表示が
    変わるため、「二重化（メイン）」「メイン単独」「サブ単独」「電源オフ（表示なし＝l1もl2も空）」を
    初期サンプルとして持つ）。文字列中の`{time}`だけが現在時刻に置き換わる（`applyIdleTokens()`）。
    いま選んでいる画面は`curIdleId`（レンダリングとは無関係なローカル変数、共有設定には入れない）で持ち、
    `#sim-idle-view`（`renderIdleSelect()` / `onIdleSelect()`）で切り替える。`simDefaultScreen()`は
    `curIdleScreen()`が返す画面を表示し、CLEARを押したときや操作の選択・リセット時にも呼ばれる。
    旧バージョン（`simIdleL1`/`simIdleL2`という単一の待機画面だった頃）の設定は`normalizeSettings()`で
    1件だけの`idleScreens`に自動移行する。
  - **コマンドごとに「この操作を始める前の待機画面」を指定できる**（`c.idleScreen` = `idleScreens`のid、
    空なら「今選んでいる画面のまま」）。操作によって実機の前提状態が違う（サブ単独のときしか行わない
    操作、など）ため追加した。`onCmdSelect()`でコマンドを選んだ瞬間に`curIdleId`を書き換え、
    `#sim-idle-view`の選択も合わせて更新する。**この選択欄（`.cm-idle-inline`）はコマンド一覧の見出し
    （`.gy-head`）に直接置いてあり、コマンドを開かなくても選べる**（詳細欄の中に置いたら「コマンド1の
    ときにどの待機画面か、開かないと分からない」と利用者から指摘されたため、見出しへ移動した）。
    名前欄と同様`onclick="event.stopPropagation()"`で、選んでも折りたたみが開閉しないようにしてある。
  - **表示部（`.sim-lcd-box`）の大きさは内容に関係なく常に固定**（`width:211px`を`.sim-screen`に、
    `.sim-lcd-box`は`height:64px`固定。以前は`flex:0 1 220px`で縮む余地があったのをやめた）。
    折り返しはせず（`white-space:nowrap`）、長い文字は`fitLcdLine()`が`scrollWidth`を見ながら
    文字サイズを`LCD_FONT_MAX`(17px)から`LCD_FONT_MIN`(8px)まで0.5px刻みで縮めて枠内に収める
    （`simSetScreen()`が`l1`/`l2`を書き換えるたびに呼ぶ）。**表示部の大きさ自体を変えて対応しないこと**
    （利用者から「サイズが変わるのは嫌」と明示されたため）。
    **固定サイズの具体的な数値（幅・高さ・`LCD_FONT_MAX`）は調整してよい**（サイズ自体を動的に
    変えないというルールと、決め打ちの数値をずっと変えないというルールは別）。**変遷**：
    ①最初は幅270px・高さ54px・15px。②利用者の指定「右側の余白が少し気になるので、少し縮めて、
    縦にはもう少し広げることができるので調整してほしい。画面はなるべくピッタリにしたい。
    縦を広げたら、文字も少し大きくするとよい」を受けて、幅270→250px・高さ54→64px・文字15→17pxに
    調整。③電源スイッチを画面幅にかかわらず常に横並びにした結果、スマホの実機で「SA-2610」の
    末尾が欠けて見える不具合が報告され、幅を250→224pxまでさらに縮めて解消した（次項参照）。
    ④利用者の指定「ディスプレイの右側の黒の余白が狭すぎる。黒の余白増やしてほしい」を受けて、
    `.sim-top`の右パディングを3→16pxに広げ、その分`.sim-screen`を224→211pxへさらに縮めて
    右側に黒いベゼルの余白を確保した（`.sim-top{padding:3px 16px 3px 3px;}`。左・上・下は
    最小限のまま、右だけを広げる）。
  - **実機で見つかった不具合と根本原因（今後同種の変更をするときは必ず踏まえること）**：
    利用者からスマホの実機スクリーンショットで「右側の画面がはみ出してる」と報告があった。
    調べると、`document.body.scrollWidth`（ページ全体の横スクロール判定）は0のままだったのに、
    **`.sim-panel`（機器の白い外枠）自体が`.card`の枠より右へあふれ、かつその中の`.sim-screen`
    （固定幅の表示部）が黒いベゼル`.sim-top`の右端よりもさらに右へあふれていた**。
    原因は`.sim-panel{min-width:0;}`（電源スイッチを追加した回にflexの見た目を保つため付けていた）。
    `min-width:0`は「中身より小さく縮んでよい」という指定なので、`.sim-with-switch`の中で
    `.sim-pwrsw`（`flex-shrink:0`で縮まない）と`.sim-panel`を並べたときに空きが足りないと、
    `.sim-panel`（→`.sim-top`→`.sim-screen`)側だけが中身の必要幅より小さく圧縮され、
    **中の固定幅`.sim-screen`（250px、縮まない設定）がベゼルの外へ静かにあふれる**。
    あふれた部分は`.card`の外側（ページの余白）に収まっていたため`document.body.scrollWidth`は
    増えず、**「横スクロールが出ない＝安全」という以前からのチェックだけでは検知できなかった**。
    **対策**：`.sim-panel`から`min-width:0`を削除し、既定の`min-width:auto`（中身より縮まない）に戻した。
    これにより、幅が足りない場合は中で静かにあふれるのではなく、`.sim-panel`自身が正しく
    必要な幅を取ろうとする＝入らなければ`document.body.scrollWidth`が正直に伸びて検知できる状態にした。
    そのうえで、`.sim-panel`/`.sim-top`のpadding・border・`border-radius`、電源スイッチ
    （`.sw-rocker`の幅・`.sw-cap`の文字サイズ）、`.sim-screen`の固定幅（250→224px）を切り詰めて、
    アプリの標準テスト幅（390px）で**`.sim-panel`が`.card`の内側に収まり、`.sim-screen`が
    `.sim-top`の内側に収まる**ことを、単なる`document.body.scrollWidth`チェックだけでなく、
    **各要素どうしの位置関係（`getBoundingClientRect()`の比較）でも確認する**ようにした
    （`document.body.scrollWidth`だけのチェックは、この種の「内側で静かにあふれる」不具合を
    見逃すことがあるため、これ以降ネスト構造を変えるときは要素間の位置関係も必ず確認すること）。
    **`.sim-panel`に`min-width:0`を再び付けないこと。**
  - **`.sim-panel`の`border-radius`は必ず「枠(border)＋内側の余白(padding)」以下にすること。**
    大きいと、外側の丸い角の曲線が内側の黒いベゼル（`.sim-top`）の角にわずかに重なり、
    「SA-2610」等の右端の文字が背景色の境目で読みにくくなる（薄い灰色の文字が、黒地から
    白っぽい`.sim-panel`の地色に切り替わる場所でほぼ同色になり欠けて見える）。
    現在は`border:1px`+`padding:4px`=5に対して`border-radius:4px`（`.sim-top`側も同様に
    `border:1px`+`padding:3px`=4に対して`border-radius:4px`）。
  - **設定タブの編集UIは折りたたみ式**（`.gy-item`/`.gy-head`/`.gy-body`を再利用、開閉状態は`cmdOpenSet`
    というレンダリングとは無関係な変数で持つ）。見出しは「コマンド1」＋インライン編集できる操作名だけを表示し、
    見出しの残り（キャレット・「コマンドN」ラベル）をクリックすると詳細（説明・待機画面・手順・CSVボタン）が開く。
    名前欄と操作ボタンは`onclick="event.stopPropagation()"`でクリックしても開閉しないようにしてある。
    「＋ コマンドを追加」ボタンは`#cmd-list`の直前（カード見出しではなく）に置く。待機画面セクションが
    先に長く続くため、カード見出しに置くとスクロールしないと押せなくなるという指摘を受けて移動した。
    待機画面のほうは`.is-row`という単純なリスト（名前・1行目・2行目、`renderIdleScreenList()`）で編集する
    （項目が少なく折りたたみにするほどでもないため）。
  - **コマンドは複製できる**（`duplicateCommand()`）。多くの手順が途中まで同じであるため、既存のコマンドを
    複製してから一部だけ書き換えられるようにしてある。複製は元の直後に挿入され、名前に「（コピー）」が付く。
  - **押し忘れたボタンをあとから途中に差し込める**（`insertCmdStep(cid, afterIndex)`）。各手順行の「＋」
    （`aria-label="この後に1つ追加"`）でその手順の直後に、一覧先頭の「＋ 先頭に追加」（`afterIndex=-1`）で
    最初に、既定値（`9`、通常押し）の新しい手順を挿入する。`addCmdStep()`（末尾に追加）とは別物。
    挿入後は`moveCmdStep()`（↑↓）と組み合わせて自由に並び替えられる。
  - **「押しまま」（`stayDown`）は、長押し・連続と排他な3つ目の手順タイプ**：離すのを待たずに、
    押した時点（`simDownKey()`内の`checkStayDownMatch()`）で次のステップへ進む。Fキーを押したまま
    数字キーを押す、のような「あるキーを押さえたまま次を押す」操作を、離すタイミングを気にせず
    案内できるようにするための機能。ハイライト色は蛍光グリーン（`.stay-mode`）。同時押し（2キー）と
    組み合わせても動作する（`simGroup`が両方のキーを含んだ時点で一致するため）。
  - **「押しまま」のキーが物理的に押されたまま、次のステップのキーを押す操作にも対応している**
    （利用者の指摘：「実行を押したまま電源オンや、メニューを押したまま電源オンの判定がうまいこと
    いっていない」）。①`実行`を押しままにする→②実行を離さずに電源スイッチONを押す、のような
    2ステップの手順（①`keys:['実行'], stayDown:true` ②`keys:['PWRSW_ON']`）で、以前は①の判定に
    使ったキー（`実行`）が物理的に押されたまま②を押しても、②の照合が「実行＋電源スイッチON」の
    組み合わせになってしまい、登録した②（電源スイッチONだけ）と一致せず、いつまでも判定が
    合わなかった（電源スイッチのように離すタイミングが実行キーの位置と離れているキーとの組み合わせで
    特に起きやすい）。**`simCarry`（押しままで既に消費済みのキー）を導入し、`simLiveKeys()`
    （`simGroup`から`simCarry`を除いたもの＝まだ消費していないキー）だけで次のステップを
    判定するようにして解消した**：
    - `checkStayDownMatch()`は`simGroup`ではなく`simLiveKeys()`で一致を見て、一致したキーを
      `simCarry`に記録する（＝消費済みにする）。
    - `simUpKey()`は、まだ消費していないキー（`simCarry`に無いキー）が他に押されている間だけ待ち、
      すでに消費済みのキーが押されたままでも待たない。離すのを待たずに次のステップへ進んだキーが
      その後いつ離れても、何も起きない（`live.length === 0`なら`return`）。
    - 「長押し」の判定（`SIM_HOLD_MS`以上押し続けたか）も、消費済みでない今のキーの組み合わせが
      始まった時刻（`simLiveStart`）を基準にする。**セッション全体の開始時刻（`simGroupStart`）を
      使うと、実行を押しままにしてから電源スイッチに手を伸ばすまでの時間がそのまま「長押し」と
      誤判定されてしまう**（`simGroupStart`は長押し中…のグロー表示など、セッション全体に対する
      演出だけに使い、個々のステップの長押し判定には使わないこと）。
    - この2ステップの並びは、①電源スイッチが先に離れても、②実行キーが先に離れても、どちらの
      離す順序でも正しく判定できる（`cmdpwrsw.js`で両方確認済み）。
  - **CSVで一括登録・書き出しができる**（`exportCommandsCsv()` / `importCommandsCsv()` / 手書きの`parseCsv()`）。
    列は「コマンド名,説明,役立つ場面,待機画面,ボタン1,ボタン2（同時押し）,長押し,押しまま,連続回数,
    画面1行目,画面2行目,結果コメント」で、同じ「コマンド名」の行が連続していると1つの操作としてまとめる。
    「待機画面」列は**名前**で指定し、読み込み時に`settings.idleScreens`の`name`と照合してidに解決する
    （一致しなければ「今選んでいる画面のまま」扱いになる）。書き出しはBOM付き（Excel文字化け対策）。
    **読み込むと`settings.commands`は丸ごと置き換わる**（`confirm()`で確認してから）。待機画面一覧自体はCSVの対象外。
  - **実際のシミュレーターのボタン操作を記録して、そのままコマンド一覧に登録できる**
    （利用者の依頼：「実際のシミュレーターのボタン操作を記録させて、その文言を後から修正できる
    ように、コマンドの記録機能があると便利。シミュレーターの上に『コマンド記録開始』という
    ボタンを用意し、それを押した後は画面操作を1、2、3という順で覚えていき、『記録停止』ボタンを
    押せばそこまでがコマンド一覧として記録される」）。
    - シミュレーター（`#pane-cmd`）の一番上、キーパッドより前に「🔴 コマンド記録開始」ボタン
      （`#btn-cmd-record`、`toggleCmdRecord()`）を置く。管理者ロック中でないと押せない
      （`requireAdmin()`）。押すと今選んでいる練習中の手順（あれば）を外し、まっさらな状態から
      記録を始める（`startCmdRecord()`）。
    - **記録中は`cmdRecording`フラグを立てるだけで、キーの押し方そのもの（同時押し・長押し・
      押しまま検出・電源スイッチ）は一切変えていない。** `simEmit(step)`の一番先頭で
      `if(cmdRecording){ recordCmdStep(step); return; }`として、既存の「練習中の手順と照合する」
      「登録済みコマンドと自動で照合する」処理より前に横取りしているだけ（`cmdRecordSteps`に
      `{keys, hold}`を1件追記する）。記録開始時に練習中の手順（`cmdSeq`）を外しているので、
      `checkStayDownMatch()`（`cmdSeq`が無いと何もしない）が横から手順を進めてしまうことも無く、
      押しっぱなしのキーを含む操作もすべて離れた時点で1件として`simEmit`経由で記録される。
    - 記録中は件数と一覧（`#cmd-record-status` / `#cmd-record-list`）がその場に増えていく
      （`renderCmdRecordUI()`）。「⏹ 記録停止」（同じボタン）を押すと`stopCmdRecord()`が
      `settings.commands`に名前「記録した操作」・`steps`だけを詰めた新しいコマンドを追加し、
      プルダウンでそれを選んだ状態にする。**このシミュレーターは登録済みの手順を再生するだけで、
      未登録の操作をした結果の画面までは分からないため、記録した手順の`l1`/`l2`（画面文言）・
      名前・使いどころ（`usage`）は空のままにしてある。** 記録後は「手順を追加・編集」
      （設定タブの既存のコマンド編集）から、利用者が文言を書き足す前提。
    - 何も押さずに記録を停止した場合はコマンドを増やさず、トーストで案内するだけにする。
  - **「簡易手順」（`#cmd-textlist`）の文字色は濃くしてある**（`.cmd-textlist{color:var(--ink);font-weight:700;}`）。
    以前は淡いグレーで、利用者から「色が薄すぎてはっきり見えない」と指摘されたため濃くした
    （画面表示部分の`.tl-scr`だけは本文より少し控えめな濃さ`#3A3D40`のまま、太字は保つ）。
    **これより薄い色に戻さないこと。**
  - **コマンドごとに「この操作が役に立つ場面」を書ける**（`c.usage`、任意）。手順そのもの（何を押すか）
    とは別に、「なぜこの操作をするのか」を管理者があらかじめ書いておける欄。`#cmd-usage`に
    プルダウンで選んだ瞬間に反映され、💡付きの淡いアンバー色の帯（`.cmd-usage`）で表示する
    （書かれていなければ`.cmd-usage:empty{display:none;}`で欄自体を出さない）。設定タブのコマンド編集
    （開いた詳細欄）に1行の入力欄を置き、CSVの「役立つ場面」列（コマンド名・説明の次、待機画面の前）
    でも読み書きできる。**利用者の依頼**：「操作するコマンドがどういう時に役に立つのかということを
    明記する、入力する画面が欲しい」という要望で追加した。
  - **画面の枠外に「電源スイッチ」のイメージを置いた**（`.sim-pwrsw` / `#sw-rocker`）。
    **利用者の依頼**：「画面の枠外で構わないから、電源のオンオフスイッチみたいな画面のイメージを
    配置してほしい。何かのボタンを押しながら電源オン、というようなコマンド操作もあり得るので。
    スイッチのイメージは上オン、下オフになる」という要望で追加した。**上＝ON・下＝OFF**（`SIM_SWITCH_KEYS`
    の`PWRSW_ON`/`PWRSW_OFF`）。「何かのボタンを押しながら」という要望どおり、**キーパッド本体の
    キーと完全に同じpress/hold/同時押し/押しままの仕組み**（`simDownKey()`/`simUpKey()`/
    `checkStayDownMatch()`）に乗せている。**画面の幅にかかわらず、常にシミュレーター画面の左に
    横並びにする**（`.sim-with-switch`。**変遷**：①最初は横並びのまま実装 → スマホ幅(390px)で
    固定幅270pxの表示部と衝突して横スクロールが出る不具合が見つかり、②`.sim-panel`本体が
    横並びになる760pxの基準に揃えて「広い画面は横並び・狭い画面は画面の上に縦積み」に変更
    → ③利用者から「スマホの際、電源ボタンが変な位置にある（縦積みだと余白ができて浮いて見える）
    ので、スペースを考えて横に並べて配置して」と指摘され、現在の**常に横並び**に戻した。
    ③に対応するため、表示部を270px→211pxに縮め（後述のLCDサイズ調整と共通）、`.sim-top`
    `.sim-panel``.sim-with-switch``.sw-rocker`の余白・枠線を切り詰めて、アプリの標準テスト幅
    である390pxで**余裕を持って**（`.sim-panel`が`.card`の枠内に収まるだけの実測の空きを
    確保して）収まるようにした（それより極端に狭い画面は対象外。詳しい不具合の経緯は前項
    「実機で見つかった不具合と根本原因」を参照）。そのため：
    - `simKeyDef(id)`を`SIM_KEYS`と`SIM_SWITCH_KEYS`の両方を見るように拡張した。ラベル表示
      （`simKeyLabel()`/`simStepLabel()`）はこの関数経由なので、電源スイッチも通常キーと同じ
      表現で手順チップ・簡易手順に出る。
    - `simKeyEl(id)`は`#sim-keys`の中だけを見るスコープ付きだったのを、`data-key`属性を持つ要素なら
      どこでも見つけられるよう`document.querySelector('[data-key="..."]')`に広げた
      （電源スイッチのボタンは`#sim-keys`の外にあるため）。
    - 手順編集のキー選択プルダウン（`simKeyOptions()`）に「電源スイッチ（枠外）」という
      `<optgroup>`を追加し、通常のキーと同じ並び（`keys`配列の1つとして）で選べるようにした。
      これにより`['PWRSW_ON','7']`のような**電源スイッチ＋通常キーの同時押し**もそのまま組める。
    - 押した瞬間（離すのを待たない）に見た目（`.sw-active`）と表示部のLED（`#led-power`他）
      を切り替える（`updatePwrSwitchVisual()`）。表示用の状態は`simPwrSwitchPos`（`'on'`/`'off'`、
      共有設定には入れないローカル変数。初期値`'on'`）。
    - **表示部のLED（電源／オンライン／通信中／エラー／コントローラ）は、個別の状態を再現せず、
      電源オフのとき以外はすべて緑点灯にする**（利用者の指定：「ディスプレイのLED表示だが、
      電源オフ以外はすべて緑点灯状態のマークに変えといてほしい」）。以前は電源とオンラインだけ
      HTMLに`on`クラスを固定で書いていたが、`led-online`/`led-comm`/`led-error`/`led-ctrl`の
      idを振り、`SIM_LED_IDS`にまとめて`updatePwrSwitchVisual()`側で電源状態に合わせてまとめて
      切り替えるようにした。個別の状態（通信エラーなど）をシミュレートする機能ではない。
    - `clearCmdHighlight()`に**既存の不具合**（`'stay-mode'`クラスの消し忘れ、`#sim-keys .simkey`
      にしかセレクタが及んでおらず電源スイッチのハイライトが消えない）を見つけて修正した
      （`'#sim-keys .simkey, .sw-cell'`に広げ、`'stay-mode'`をクラス除去リストに追加）。
    - 見た目は実機の側面スイッチのイメージなので、アプリの2色ルールの対象外（キーパッド本体と同じ扱い）。
  - **`.sim-panel`まわりの見た目は実機の再現なので、アプリの2色ルール（濃い灰色×濃い赤）の対象外**
    （機器写真やウェルシスのロゴと同じ扱い。ゴールドの`実行`キーなどは実機の色）。
    ただし**「いま押すボタン」のハイライトだけはアクセント色（`--sky`）**にして、アプリ全体と揃える。
    画面が広いとき、`.sim-panel`は`width:fit-content`にして中身ぶんの幅だけを取る
    （`.sim-top`＋`.sim-keys`の合計より広い余白がカードの右側に余ってしまう不具合があったため）。
- **「📄 レポートの見方」タブは、実機が印字する「MONSTERA インストールレポート」の読み方を
  たどるためのナビゲーション画面。** 利用者から渡されたExcel「MONSTERAインストールレポートデータの
  見方」（`98レポート`シート）の内容をそのままデータ化したもの。
  - **データは`REPORT_GUIDE`としてコードに固定で持つ。共有設定（`settings.json`）には入れない**
    （実機レポートの仕様そのものであり、担当者ごとに変える性質のものではないため）。
  - **レポートは章ごとに分けず、実機の印字どおり1本の縦長でそのまま出す**（利用者の指定：
    「項目ごとに分けるのではなく、レポートの一覧をそのまま表示して」「レポートは縦にそのまま表示」）。
    最初は章を切り替えるチップ（`.rg-chip`）方式だったが、**分けずに通しで出す形に作り直した。
    章切り替え方式に戻さないこと。** 全章の行は`repBuildFlat()`で1つの配列（`repFlat`）に平らに
    並べてから描画する（`renderRepAll()`）。**レポートそのものの桁は動かさない**。
  - **見た目は「キッチンプリンターが打ち出したレシート」にする**（利用者の指定：
    「インストール情報はキッチンプリンターから印刷されるレシートみたいな印字なので、画面のデジタル
    チックな画面というよりは、もう少し印刷物のイメージで作成してほしい」＝利用者が添付した実機印字の
    写真）。**印字例(`#rep-sample`)は白黒のみ**（`.rg-tok`の地色は塗らず、押せる値には薄い下線だけを
    付ける）。周りは薄いグレーの台紙（`.rg-sample-wrap`）にして、その上に白い紙（`.rg-sample`。
    `max-width:560px`で中央寄せ、薄い影）が乗っている構図にする。
    ハイライトは黒背景で塗ると印刷物らしさが消えるため、**蛍光ペンでマークした見た目**にする
    （`.rg-tok.on`は黄色の`background`＋下だけ`--sky`色の線。行の`.on`も薄いクリーム色`#F2F0E4`に
    留める）。**この画面のロジック（`repShowLine`/`repShowTok`/`repHlKey`等）は変えていない**
    （利用者の指定「ロジックは今のままでよい」。今回は見た目だけの作り直し）。
  - **■を敷き詰めた見出し（`repBannerHtml()`。利用者が添付した実機印字の写真）は、実機の印字どおり
    先頭の「インストール情報」だけに使う。他の章では使わないこと**（利用者の指定：「添付はTOPのみで、
    他で使用することはない」）。元Excelでも「インストール情報」の見出しだけが複数行・複数列にまたがる
    特別な結合セル（行3〜5）になっており、他の章とは扱いが違う。
    「システム情報」の見出しはExcel上も装飾のない平文（`sections[1].lines[0]`の`head:1`行）として
    すでに印字されているため、追加の見出しは要らない。ECT/MST/MPR/MD/HT設定はExcelを見ても
    見出し行そのものが印字されていない（機器の行が直接続く）ため、**この5つの章には見出しを
    追加で挟まない**（`repBuildFlat()`で`blk`を作るのは`si === 0`のときだけ）。
    右側の「項目一覧」は内部的な章の区切り（`si`）だけで判定しており、印字に見出しが無い章でも
    問題なく動く。
  - **2段階で読める**のが要点（利用者の依頼：「該当の行にマウスオーバすると説明と、該当の箇所を
    オーバーするとより細かい内容が表示されるようにしてほしい」「数字項目をマウスオーバーしたり、
    クリックしたりすると、該当の項目の説明が出るように」）。
    ①**行**にマウスを乗せる → その行の説明（`line.desc`）、②**値**に乗せる → その項目だけの
    詳しい説明（`tok.k`の項目名、または`tok.d`）。**スマホにはhoverが無いので、`mouseover`と
    `click`の両方を同じハンドラにつないでタップでも同じように動かすこと**（`wireRepEvents()`）。
    通しで出すようになったため、**記号（A・B・C…）の連動は同じ固まりの中だけに限る**
    （`repHlKey(si, k)`。章をまたいで同じ記号が光ると別物を指してしまうため）。
    右側の「項目一覧」と注意書きは、**いま指している固まりのものだけ**を出す（`repShowSideFor(si)`）。
  - **印字例の桁位置（`tok.p`）は元Excelの列位置をそのまま使っている。** 実機のレポートは
    項目どうしが隙間なく並ぶ場所がある（例：最終接続日時と現メニューバージョン）ため、
    **桁をずらすと「どの位置が何の値か」が分からなくなる**。等幅フォント＋`white-space:pre`＋
    `repWidth()`（全角を2桁で数える）で実物と同じ桁に並べること。**桁をずらさないこと。**
    隣り合う項目が見分けられるよう、値は1つおきに背景の濃さを変えている
    （`.rg-tok.pick:nth-of-type(even)`）。
  - `fields`（A・B・C…の記号と中身）がある章では、**値の上に記号を小さく出し**（`.rg-key`）、
    値と「項目一覧」の行が**相互にハイライトで連動する**（`repHlKey()`）。
  - **狭い画面では「説明」を印字例より上に出し、画面の上端に貼り付ける**（`.rg-detail-wrap`の
    `order:-1`＋`position:sticky`。`.rg-side`は`display:contents`で1列に流し込む）。
    **広い画面でも、stickyにするのは`.rg-detail-wrap`だけにする**（`.rg-side`全体には付けない）。
    項目一覧が長い操作（MPRの21項目など）で`.rg-side`全体の高さが画面の高さを超えると、
    CSSの仕様上sticky要素はスクロール中に追従が外れてしまう不具合が実際に発生したため。
    説明（`.rg-detail`）は常に短いので単独でstickyにすれば問題なく、項目一覧（`.rg-fields-wrap`）は
    普通に流れるままにする。
    印字例が長いので、下の方の値をタップしたときに説明が画面外だと読めないため。
    **`.rg-body`（広い画面でのflexの行）には`align-items:flex-start`を付けないこと。**
    付けると`.rg-side`の高さが自分の中身（説明＋項目一覧）だけになり、印字例（`.rg-left`）より
    短いままになる。すると印字例をそのぶんスクロールしただけで`.rg-side`自体が画面より上に
    完全に流れてしまい、中のsticky説明もいっしょに見えなくなる（利用者の指摘：「パソコンでは
    対象をマウスオーバー、クリックした際、右の説明が良く見えない」＝実際は「見えない」だった）。
    `align-items`は既定の`stretch`のままにして、`.rg-side`の高さを常に`.rg-left`と同じ高さまで
    伸ばすことで、印字例をどこまでスクロールしても説明が追従できるようにしている。
    説明パネル（`.rg-detail`）自体も、影（`box-shadow`）を付けてはっきり浮かせ、文字サイズも
    13px→14px相当に少し大きくして目立たせている。
  - **「注意事項」（`settings.reportNotes`）は、利用者がこの画面から直接、管理者パスワードで
    追記できる**（利用者の指定：「インストール情報レポートのところも…注意事項を登録できるように
    してもらえるとありがたい」「設定からではなく、インストールレポートの画面から管理者パスワードを
    入れれば、追記できるようにしてほしい」）。`REPORT_GUIDE`（実機レポートの固定仕様）とは別物で、
    **共有設定`settings.json`に入れる**（店舗ごとに気づいた注意点を運用中に増やしていくものなので）。
    `#rep-lead`の直下（印字例より上、章によらず常に見える1箇所）に📌カードとして置き、
    `entryNotes`/`midNotes`と同じ`{id, text}`の配列として持つ。
    **設定タブを経由せず、この画面自体にパスワード入力欄を用意している**（`unlockFromRep()`。
    設定タブの`unlockSettings()`と中身は共通の`doAdminUnlock(v, onDone)`を呼ぶだけの薄いラッパーに
    分け、パスワード確認〜`sessionStorage`保存のロジックを重複させないこと）。**ロックの状態は
    アプリ全体で1つ**（`isAdminUnlocked()`は`sessionStorage`を見るだけなので、レポート画面で
    解除すれば設定タブも解除された状態になる。逆も同じ）。閲覧（一覧表示）は誰でもでき、
    追加（`addReportNote()`）・削除（`removeReportNote()`）だけ`requireAdmin()`で守る。
- **タブが5つになったので、狭い画面（600px未満）では設定タブを歯車マークだけにする**
  （`#tab-set`を`flex:0 0 auto`にして幅も内容ぶんに詰める。利用者の指定：「上部タブがスペース
  なければ設定は歯車マークでも大丈夫」）。600px以上では他のタブと同じく「⚙️ 設定」と出し、等幅に戻す。
  他のタブの文字を削るときも同じ`.only-wide`の仕組みを使うこと。
- **設定タブは常時表示を最小限にする。** 使用頻度の低い項目（共通設定・作業当日の目印・カレンダー連携）は
  「詳細設定」1枚に折りたたむ（既定は閉）。新しい設定項目を追加する場合も、まず「詳細設定」に入れることを検討する
  （毎回必ず調整するような項目だけを常時表示に置く）。
- **アプリ名は「OES入替作業APP」で統一する**（`<title>`・ヘッダー・マニュアル・各ドキュメント）。
  カレンダーの予定タイトル `OES入替作業({業態} {店舗名})` はこれとは別物なので変更しない。
- 機器画像はタイトル横に**小さく控えめに**置く（高さ34〜38px）。主張させない。
- **`index.html` の仕様を変えたら `manual.html` と `SPEC.md` も必ず更新する。**
- **`manual.html` は「かんたん版」（利用者の指定で簡略化済み）。** 目次や詳細な操作手順・FAQ・
  デプロイ手順などを積み上げて元の分量に戻さないこと。**構成は「1タブ＝1見出し」だけに固定する**
  （タブが増えたときだけ見出しを増やす。それ以外の理由で見出しを足さない）。現在は次の5見出し。
  1. Googleカレンダーに登録する（📅 カレンダー）
  2. 当日の作業に役立つ機能（🛠️ 作業当日）
  3. 保守機能の操作を確かめる（📋 コマンド一覧）
  4. レポートの見方（📄 レポートの見方）
  5. 設定の項目（⚙️ 設定）
  管理者パスワードの運用（config.php等のサーバー設定）やデプロイ手順など開発者向けの内容は
  `README.md` / `SPEC.md` 側に書き、`manual.html` には書かない（利用者向けの使い方だけに絞る）。
  **`manual.html` に管理者パスワードの具体的な値（初期パスワードを含む）を書かないこと。**
  `manual.html` は誰でもブラウザで開ける公開ページのため、値を書くと管理者以外にも見えてしまう
  （利用者からの指摘で削除済み。「パスワードは管理者にお尋ねください」に留める）。

## 画面構成（5タブ）

1. **📅 カレンダー** — 質問形式のウィザードで、日付選択・入力・Googleカレンダーへの登録までをこの1画面で完結する
   （旧版の「登録リスト」タブは廃止し、このタブに統合済み。**タブを分ける設計に戻さないこと**）。詳細は上の
   「重要なルール」を参照。
   - ①「業態と稼働件数を選ぶ」カード
   - ②「日付・店舗名を入力」カード: 件数ぶんの行に、1件ごとに日付・開始時刻・終了時刻・店舗名・住所を
     まとめて入力し、「このN件を登録リストへ」で③へ追加
   - ③「一覧で確認してGoogleカレンダーへ登録」カード: 登録リスト（既定は折りたたみ）。「Googleカレンダーで順番に登録」
     （`startQueue()`）と「ICSファイルで一括取込」（`downloadIcs()`）を配置する。
2. **🛠️ 作業当日** — **主動線は「業態をプルダウンで選び、店舗名を入れるだけ」**（`#day-gyotai` /
   `#day-tenpo` →「この内容で連絡文を作る」＝`buildDayFromGyotai()`）。その業態・時間帯の定型文から
   入店/中間報告/退店を作る。時間帯（`#day-slot-field`）は**その業態の時間帯が2つ以上あるときだけ**出す。
   **業態を選んだ時点で、その業態の「手順書・資料」（`g.manuals`）が決まる**
   （`#day-gyotai-manuals`。表示位置は「この内容で連絡文を作る」ボタンの下・「クリア」ボタンの上。
   詳細は「重要なルール」の該当項目を参照）。
   Googleカレンダーの説明文の貼り付けは、同じカード内の折りたたみ（`#gy-paste`）に入れた**補助動線**。
   各欄のコピー→「今日の作業」カードの「Google Chat を開く」（`#btn-open-chat`）でその業態・店舗のGoogle Chatを
   開いて貼り付ける（①②③個別のChatボタンは廃止済み。同じボタンは画面下の「よく使う文」欄の直前にもある）。
   画面下に「よく使う文」の小さなコピーチップを置く（**控えめな見た目を維持する**。カード化しない）
3. **📋 コマンド一覧** — 実機（SEIKO SA-2610 マルチステーション）の操作パネルのシミュレーター。
   タイトルは「Monstera/Relier シミュレーター」。**1枚のカードにまとめてあり、①②のような番号区切りは無い**
   （シミュレーター本体 → すぐ下に操作のプルダウン`#cmd-select` → 役立つ場面・手順・簡易手順、の順）。
   詳細は「重要なルール」の該当項目を参照。
4. **📄 レポートの見方** — 実機が印字する「MONSTERA インストールレポート」の読み方を、
   実物と同じ桁で並べた印字例の上でたどれるナビゲーション画面（`#pane-rep`）。
   詳細は「重要なルール」の該当項目を参照。
5. **⚙️ 設定** — 管理者ロック＋共有設定の状態表示、業態・時間帯・定型文（並び替え可）、
   業態ごとの注意事項・手順書資料（画像・PDF）、よく使う文、
   **コマンド一覧（保守機能の操作手順）**、
   作業当日の目印、Googleカレンダー連携（停止中・項目のみ）、
   共通設定、共有設定の保存、アプリとしてインストール

### 作業当日タブの方針
- **「業態を選ぶ＋店舗名を入れる」だけで完結することを主役にする。** カレンダーもコピーも不要で使えること。
  この動線が目立たなくなるような画面変更をしないこと（貼り付けは折りたたみの補助動線に留める）。
- **Google Chat への自動送信はしない。** コピー＆手動貼り付け。誤送信を防ぐためこの方針を維持する。
- **Apps Script 連携は任意機能。** 未設定でも「説明文の貼り付け」だけで全機能が使えること（この前提を壊さない）。
- `apps-script/Code.gs` と `index.html` の `APPS_SCRIPT_CODE` は**同じ内容**。片方を直したらもう片方も直す。

## 主な編集ポイント（index.html内）

| 関数・定数 | 役割 |
|---|---|
| `defaultSettings()` | 初期の業態・時間帯・定型文。既定値の変更はここ |
| `TPL_STANDARD` / `TPL_SUKIYA` / `TPL_STAFF` | 定型文のひな形 |
| `normalizeSettings(o)` | 読み込んだ設定の正規化。**設定項目を増やしたらここも必ず更新** |
| `fillTemplate(tpl, ctx)` | 差し込み文字の置換。廃止した `{担当者}` 系は空文字にして消す |
| `buildDesc(en)` / `buildTitle(en)` | 説明文・タイトルの生成 |
| `renderWizardForm(keepValues)` | ①業態・稼働件数のプルダウンを描画し、②の行を再構成する（`renderStoreRows()`を呼ぶ） |
| `onWizGyotaiChange()` / `updateWizardHints()` | 業態変更時の時刻ヒント（午前・午後の時刻）更新 |
| `onWCountChange()` / `renderStoreRows()` | 稼働件数の変更に合わせて②の入力行（`.item-row`）を過不足なく増減する |
| `slotNearestTime(g, hhmm)` | 開始時刻に一番近い時間帯を返す（どの定型文を使うかの判断） |
| `wizDefaultTimes()` / `applyGyotaiTimesToRows()` / `fixRowTimes()` | ②の行の初期時刻、業態変更時の入れ替え、開始＞終了の補正 |
| `applyFirstRowTimeToOthers()` / `firstRowTimesOrDefault()` | 1件目の開始・終了時刻を2件目以降にコピーする（既存行への反映／新規行の初期値） |
| `updateWizardNav()` / `goStep(n)` / `setBadge()` / `setNext()` | ①②③の進捗バッジと「次へ」の案内 |
| `applyGyotaiTimesToDay()` / `onDayTimeChange()` / `clearDayAll()` | 作業当日タブの時刻指定とクリア |
| `updateDayGyotaiNotes()` | 業態ごとの「注意事項」（`g.notes[]`）を、`place`ごとに`#day-gyotai-notes-in/-mid/-out`へ振り分けて表示。該当なしは非表示 |
| `addGyNote(gid)` / `removeGyNote(gid,nid)` / `setGyNotePlace(gid,nid,place)` / `setGyNoteText(gid,nid,v)` | 設定画面での注意事項の追加・削除・配置変更・編集 |
| `normalizeGyotaiNotes(raw)` / `trimLines(s)` | 注意事項の正規化（旧・文字列形式からの移行を含む）／各行の前後空白除去 |
| `addGyManualClick(gid)` / `onGyManualFileChange(gid,inputEl)` / `uploadGyManual(gid,file)` | 設定画面での手順書・資料の追加（隠しファイル入力を開く→検証→`manual-upload.php`へアップロード） |
| `removeGyManual(gid,mid)` / `deleteGyManualFile(url)` | 手順書・資料の削除（一覧から即除去＋共有設定を保存／サーバー上の実ファイル削除は失敗しても無視） |
| `normalizeGyotaiManuals(raw)` / `findGyManual(g,mid)` / `fmtFileSize(n)` | 手順書・資料の正規化／検索／サイズ表示（「1.2MB」等）用の整形 |
| `updateDayGyotaiManuals()` | 作業当日タブで選んだ業態の手順書・資料を`#day-gyotai-manuals`へ一覧表示。該当なしは非表示 |
| `openManualWindow(url)` | 手順書・資料を別ウィンドウで開く（`window.open()`を明示的に呼び、失敗時だけ通常のリンク遷移にフォールバック） |
| `manualHref(m)` | 手順書・資料のリンク先URLを種類ごとに決める（画像は`m.url`を直接、PDFは`manual-view.php`経由） |
| `restoreAdminPw()` | 起動時、ロック解除状態が残っていれば`sessionStorage`から`adminPw`（平文パスワード）を復元する |
| `renderEntryNoteRow()` / `onEntryNoteToggle()` / `rebuildEntryNoteBlock()` / `stripEntryNoteLines()` | ①入店連絡に足す文（選択式） |
| `renderEntryNoteList()` ほか | 設定画面での「入店連絡に足す文」の追加・編集・並び替え・削除 |
| `renderMidNoteRow()` / `onMidNoteToggle()` / `rebuildMidNoteBlock()` / `stripMidNoteLines()` | ②中間報告に足す文（選択式。entryNotesと同じ仕組みをmidNotesに複製したもの） |
| `renderMidNoteList()` ほか | 設定画面での「中間報告に足す文」の追加・編集・並び替え・削除 |
| `dateRowHtml(i)` / `storeRowHtml(i)` | ②（日付・時間帯）と③（店舗名・住所）の1行分のHTML生成 |
| `onRowDateChange(i)` / `onRowTimeChange(i,which)` / `updateRowWhen(i)` | ②の入力を③の各行見出し（日付・時刻の表示）へ反映する |
| `submitStoreRows()` / `resetStoreRows()` | ②③を突き合わせて日付のある行だけ登録リストへ追加／登録後に②③の入力を空へ戻す |
| `toggleEntry(id)` | ③登録リストの各カードの折りたたみ開閉 |
| `entryInnerHtml(en, i)` | 登録リストのカード1件分のHTML |
| `googleUrl(en)` | GoogleカレンダーのTEMPLATE URL |
| `buildIcs()` / `icsFold()` | ICS生成（RFC5545の75オクテット折返し） |
| `renderSettings()` | 設定画面の描画 |
| `moveGyotai(i, d)` | 業態の並び替え（カレンダー画面のプルダウンの順番になる） |
| `applyLockState()` / `unlockSettings()` / `lockSettings()` / `requireAdmin()` | 設定タブの管理者ロック |
| `loadSharedSettings()` / `saveSharedSettings()` / `reloadSharedSettings()` | 共有設定（`settings.json`）の読み込み・保存 |
| `markSharedDirty()` / `updateDirtyBar()` | 未共有の変更を記録し、設定タブに警告バーを出す。保存できる環境なら自動保存を予約する |
| `probeSaveEndpoint()` | 起動時に `settings-save.php` が本当に使えるかを判定する（PHP未実行・config.php欠落・404を区別する） |
| `runDiagnostics()` / `diagRow()` / `diagFetchOk()` / `iniStrToBytes(s)` | 設定タブの「サーバーの状態をチェック」。共有されない／インストールできない／アップロード上限が小さい原因を1つずつ表示する |
| `toggleGy(id)` | 折りたたみ（業態・「詳細設定」で共通利用）の開閉。要素idは `gy-<id>` |
| `splitReport(text)` | 説明文を入店/中間報告/退店に振り分ける。目印は `settings.dayKeywords` |
| `rebuildOutBlock()` / `stripEquipmentLines(text)` / `stripRemarkBlock(text)` | ③退店連絡の本文を「本文 → 機器台数 → 特記事項」の順に組み立て直す。入力したものだけ反映する |
| `onRemarkInput(v)` / `resetDayRemark()` | 特記事項の入力と初期化（`dayRemark` / `appliedRemark`） |
| `renderEquipRow()` / `onEquipInput(code,v)` / `dayCounts` | 機器台数プルダウン（1〜10）の描画・変更ハンドラ・現在値（候補切替でリセット） |
| `openStoreChat()` | 一般のGoogle Chatを名前つきウィンドウで開くだけ（クリップボードのコピーはしない。`#btn-open-chat`と「よく使う文」欄前のボタンの2箇所から呼ぶ。呼び出し元ごとに処理を分けない） |
| `saveDraftSoon()` / `saveDraft()` / `restoreDraft()` / `clearDraft()` | 入力途中の内容の一時保存と復元（`oes-calendar-draft-v1`、24時間で失効） |
| `sha256Hex(str)` | 外部ライブラリを使わないSHA-256。管理者パスワードの照合に使う（httpsでない環境でも動くようにするため） |
| `loadAdminConfig()` / `verifyAdminPassword(v, cb)` | `config.json` の読み込みと、パスワードの照合（admin-auth.php → ハッシュの順） |
| `currentAdminPw()` | 共有設定の保存に使う平文パスワード。無ければ聞き直す（保存はしない） |
| `renderDayGyotaiForm()` / `renderDaySlotRow()` / `buildDayFromGyotai()` | 作業当日タブの主動線（業態＋店舗名から、定型文で連絡文を作る） |
| `openInstallHelp()` / `openModal()` | フッターの「アプリインストールはこちらから」。すぐ入れられるならダイアログ、無理ならOS別の手順を出す |
| `installPwa()` | `beforeinstallprompt`で保持したイベントの`.prompt()`を呼び、PWAインストールダイアログを出す |
| `renderPhraseChips()` / `copyPhrase(i)` | よく使う文のチップ描画とコピー |
| `SIM_KEYS` / `SIM_SWITCH_KEYS` / `simKeyDef(id)` / `simKeyLabel(id)` / `simStepLabel(st)` | コマンド一覧タブの実機キー配列（キーパッド／電源スイッチの2つ）と、その表示用ラベル（**idは保存データなので変えない**） |
| `renderSimKeys()` / `renderPwrSwitch()` / `wireSimKeyEvents(btn,id)` | キーパッドと電源スイッチのボタン描画・pointerイベント配線（同じ関数を両方に使う） |
| `simDownKey(id)` / `simUpKey(id)` / `simKeyEl(id)` | 押す・離すの判定（長押し・同時押しの検出もここ）。`simKeyEl`は`data-key`属性を持つ要素ならキーパッドの外（電源スイッチ）でも見つけられる |
| `simPwrSwitchPos` / `updatePwrSwitchVisual()` / `SIM_LED_IDS` | 電源スイッチの状態（`'on'`/`'off'`、共有設定には入れない）と、見た目（`.sw-active`）・表示部の全LED（`SIM_LED_IDS`＝電源／オンライン／通信中／エラー／コントローラ、電源オフ以外は全部緑点灯）への反映 |
| `cmdRecording` / `cmdRecordSteps` / `startCmdRecord()` / `stopCmdRecord()` / `recordCmdStep()` | シミュレーターの実際のボタン操作を記録し、`settings.commands`に新しいコマンドとして登録する機能。`simEmit()`の先頭で横取りするだけで、キー判定ロジックは変えていない |
| `settings.reportNotes` / `renderRepUserNotes()` / `unlockFromRep()` / `addReportNote()` / `removeReportNote()` | レポートの見方タブから直接、管理者パスワードで追記できる「注意事項」。閲覧は誰でも可、追加・削除は`requireAdmin()`で守る |
| `expandPhysicalSteps(cmd)` / `stepRepeat(st)` | 手順を「実際に押す回数ぶん」の1回押しの並びに展開する（連続押しの照合・表示用） |
| `simEmit(step)` / `simMatchProgress()` / `simShowCommand(c)` | 1回の操作が確定したときの処理。登録済みコマンドと前方一致で照合し、途中／完了の画面を出す |
| `simSetScreen(l1,l2)` / `simSetMsg(msg)` / `simSetNote(memo)` | シミュレーターのLCD表示（上下行、`fitLcdLine()`で自動縮小）・案内メッセージ・結果コメント欄 |
| `fitLcdLine(el)` / `LCD_FONT_MAX` / `LCD_FONT_MIN` | 表示部の大きさを固定したまま、長い文字だけ文字サイズを縮めて収める |
| `simDefaultScreen()` / `curIdleScreen()` / `curIdleId` / `findIdleScreen(id)` / `applyIdleTokens(s)` | 待機中の画面（複数登録から選ばれている1つを表示。`{time}`置換） |
| `renderIdleSelect()` / `onIdleSelect(v)` | シミュレーター側の待機画面プルダウン（`#sim-idle-view`）の描画と切り替え |
| `renderCmdSelect()` / `onCmdSelect()` / `renderCmdSteps()` | 手順のプルダウンとチップ（9×2 → 8 → 実行 → 完了）の描画（コマンドごとの待機画面切り替えも`onCmdSelect`が行う） |
| `renderCmdTextList()` | 手順ガイド右側の「簡易手順」（手順を文章で上から順に読めるリスト。文字は`var(--ink)`で濃く表示）の描画。`renderCmdSteps()`から毎回呼ばれる |
| `cmdGo(i)` / `cmdTryAdvance(step)` / `cmdNext()` / `cmdPlay()` / `cmdStop()` / `cmdReset()` | 手順ガイドの進行（1つ進む・自動再生・最初から。連続押しの途中カウントも`cmdTryAdvance`が持つ） |
| `simDownKey(id)` / `checkStayDownMatch()` / `simCarry` / `simLiveKeys()` | 「押しまま」の判定（離すのを待たずに、押した時点で一致していれば次のステップへ進む。`simCarry`で消費済みのキーを覚えておき、押したまま残っていても次のステップの判定に混ざらないようにする） |
| `normalizeCommands(raw)` / `defaultCommands()` | コマンド一覧の正規化と初期サンプル（保守機能モード／インストール情報／電源を切る／システムID確認／印刷サンプル） |
| `normalizeIdleScreens(raw)` / `defaultIdleScreens()` | 待機画面一覧の正規化と初期サンプル（二重化／メイン単独／サブ単独／電源オフ） |
| `cmdOpenSet` / `toggleCmdOpen(id)` / `renderCommandList()` | 設定タブのコマンド編集（折りたたみ式）の開閉状態と描画 |
| `duplicateCommand(id)` | コマンドを手順ごと複製し、元の直後に挿入（名前に「（コピー）」を付ける） |
| `insertCmdStep(cid,afterIndex)` | 押し忘れたボタンを途中（`afterIndex`の直後、-1なら先頭）に差し込む |
| `cmdPlayStepSec` / `setCmdPlaySpeed(v)` / `cmdPlayIntervals()` | 「順番に再生」の間隔（`#cmd-play-speed`で0.8〜3秒に変更可、他の間隔も比例して伸縮） |
| `setCommandIdleScreen(id,v)` | コマンドごとの待機画面（`c.idleScreen`）の設定 |
| `setCommandUsage(id,v)` | コマンドごとの「この操作が役に立つ場面」（`c.usage`）の設定。②カードの`#cmd-usage`にそのまま反映される |
| `setCmdStepStayDown(cid,sid,on)` | 手順の「押しまま」設定（長押し・連続とは排他） |
| `exportCommandsCsv()` / `importCommandsCsv(text)` / `parseCsv(text)` | コマンド一覧のCSV書き出し・読み込み（読み込むと丸ごと置き換え。列に「役立つ場面」を含む。待機画面の列は名前で照合。待機画面一覧自体は対象外） |
| `renderCommandList()` / `addCommand()` / `addCmdStep(cid)` / `setCmdStepKey(...)` ほか | 設定画面でのコマンド・手順の追加・編集・並び替え・削除 |
| `renderIdleScreenList()` / `addIdleScreen()` / `removeIdleScreen(id)` / `moveIdleScreen(i,d)` / `setIdleScreenField(id,which,v)` | 設定画面での待機画面の追加・編集・並び替え・削除（最低1件は残す） |
| `renderPhraseList()` ほか | 設定画面でのよく使う文の追加・並び替え・削除 |
| `REPORT_GUIDE` | 「レポートの見方」タブの全データ（章・印字例・説明・記号）。**共有設定には入れずコードに固定で持つ**（実機レポートの仕様のため） |
| `openRepTab()` / `repBuildFlat()` / `repFlat` | レポートの見方タブの初期化。全章の行を1つの配列に平らに並べ、章で分けず通しで出す |
| `renderRepAll()` / `repLineHtml(line,fi)` | 印字例を1本の縦長で描画。`tok.p`の桁位置どおりに空白を入れて実機と同じ桁に並べる |
| `repShowSideFor(si)` / `repCurSi` | いま指している固まりの「項目一覧」と注意書きだけを右に出す |
| `repWidth(s)` / `repSpaces(n)` | 全角を2桁として数える桁計算（等幅の印字例をずらさないために必要） |
| `repShowLine(li)` / `repShowTok(li,ti)` / `repShowDefault()` | 行にマウス→行の説明、値にマウス→その項目だけの詳しい説明、外れたら章の説明に戻す |
| `repHlKey(si,k)` / `repClearHl()` / `repFieldName(sec,k)` | 記号（A・B・C…）で印字例の値と「項目一覧」を相互にハイライト連動させる（**同じ固まりの中だけ**） |
| `wireRepEvents()` | `mouseover`と`click`の両方を同じハンドラにつなぐ（スマホにはhoverが無いためタップでも同じ動きにする） |
| `detectFromText(text)` | 本文から業態・店舗名・時刻を推測 |
| `htmlToText(t)` | HTML混じりの説明文をプレーンテキスト化 |
| `copyText(text, cb)` | クリップボードへコピー（execCommandフォールバック付き） |
| `fetchCalendar(url, date, q)` | Apps Scriptから当日の予定を取得 |
| `APPS_SCRIPT_CODE` | 設定画面で表示するApps Scriptコード |

## 差し込み文字

`{業態}` `{店舗名}` `{日付}` `{開始}` `{終了}` `{時間帯}`
（旧版互換で `★0`〜`★3` も置換される）

## 動作確認

`index.html` をブラウザで直接開けば動く（サーバー不要）。
自動テストを走らせる場合はPlaywrightで、PC（1280x900）とスマホ（390x844）の両方を確認する。
共有設定（`settings.json`）の読み込みは `file://` では動かないため、その確認だけは簡易サーバー
（例: `python3 -m http.server`）で行う。
確認観点: ①業態・稼働件数の選択、②の行ごとに異なる日付・時刻・店舗名・住所を入力してのN件一括作成
（日付未選択行のスキップ含む）、③一覧の折りたたみ表示・Google登録・ICS一括取込、定型文の編集反映、
業態の並び替えがカレンダーのプルダウンに反映されること、管理者ロック（誤パスワードで解除されない／
ロック中は変更できない／インストールと閲覧はできる）、共有設定の読み込み・保存と別ブラウザへの反映、
ICSの中身、作業当日の貼り付けからの振り分け・コピー・**2箇所ある**「Google Chat を開く」ボタン
（「今日の作業」カードと「よく使う文」欄の直前。どちらを押しても押すたびにChatを開き直すだけで、
**クリップボードは書き換えない**こと＝①②③のコピー内容が上書きされないこと）、
③退店連絡のコピーボタンが特記事項の下にあること、
特記事項欄が2〜3行程度の高さであること、機器台数プルダウン、
PWAインストールボタンの表示切り替え、横スクロールが出ないこと、JSエラーが出ないこと。
コマンド一覧タブは、プルダウンで選んだ手順がチップで出ること、「順番に再生」でキーが順にハイライトされること、
自分でキーを押しても進むこと、**◁＋▷の同時長押し**で保守機能モードの画面が出ること（自動テストでは
`PointerEvent`を2つ送って再現する）、電源長押し→もう一度押すで表示が段階的に変わること、
**同じボタンを連続で何回か押す手順**（連続回数バッジ・青のハイライト）が正しくカウントされること、
**長押し＝琥珀色／連続押し＝蛍光ブルー／押しまま＝蛍光グリーン／それ以外＝赤**の4色が見分けられること、
**「押しまま」は離すのを待たずに押した時点で次のステップへ進み、あとで離しても二重に進まないこと**
（同時押し＝2キーの組み合わせでも同様に動くこと）、
**画面に出す文字（l1/l2）が1つも無い操作は完了してもLCDが変わらず、`結果コメント`だけが案内エリアに出ること**、
**表示部（LCD）の大きさは内容にかかわらず常に同じで、長い文字は折り返さずに文字サイズだけが縮んで収まること**、
待機中の画面（`settings.idleScreens`、`{time}`トークン）が複数登録できてプルダウンで切り替えられること、
**コマンドごとに指定した待機画面が、そのコマンドを選んだ瞬間に反映されること**、
**コマンドごとに設定した「役立つ場面」（`c.usage`）が②カードに💡付きで表示され、未設定なら欄自体が
出ないこと**、**「簡易手順」の文字が薄すぎず、はっきり読める濃さで表示されること**、
**画面の枠外にある電源スイッチ（`PWRSW_ON`/`PWRSW_OFF`）が、ON/OFFどちらを押しても即座に見た目
（`.sw-active`）と表示部の全LED（電源／オンライン／通信中／エラー／コントローラ。電源オフ以外は
すべて緑点灯、オフのときはすべて消灯）の点灯状態が切り替わること**、**電源スイッチがキー選択
プルダウンの「電源スイッチ（枠外）」optgroupから選べ、通常のキーとの同時押し・押しままの手順（例:
数字キーを押したまま電源スイッチをON）が正しく一致すること**、**押しままにしたキー（例：実行）が
物理的に押されたまま次のステップのキー（例：電源スイッチON）を押しても正しく一致すること**
（キー・電源スイッチのどちらを先に離しても完了すること。押しままで消費済みのキーが後から
離れても二重に何も起きないこと）、
設定タブで待機画面の追加・編集・削除ができること、コマンドの複製（`duplicateCommand()`）が手順ごと
正しくコピーされること、設定タブのコマンド編集が折りたたみ式で開閉できること
（「＋ コマンドを追加」が一覧のすぐ上にあること）、
CSVの書き出し・読み込みが往復できること（待機画面の列は名前で照合されること）、
**「🔴 コマンド記録開始」は管理者ロック中でないと押せないこと**、**押すと選んでいた練習中の
手順が外れ、記録中の件数・一覧がその場に増えていくこと**、**単発・同時押し・長押しがそれぞれ
正しく1件として記録されること**、**「⏹ 記録停止」を押すと`settings.commands`に新しいコマンド
（名前「記録した操作」）が増え、プルダウンでそれが選ばれてチップも出ること**、**何も押さずに
停止した場合はコマンドが増えないこと**を確認する。
広い画面でキーパッドの右に余計な余白ができていないこと、
**タイトルが「Monstera/Relier シミュレーター」で、①②のような番号区切りが無く1枚のカードに
まとまっていること**、**「調べたい操作」のプルダウンがシミュレーターの直後（`#sim-msg`等を
挟まず）にあること**、**画面の枠（`.sim-top`）が黒く、LEDが緑色、LCD（`.sim-lcd-box`）が
うす緑がかった地色に黒文字で表示され、表示部の右側にはっきり分かる黒い余白があること**、
**電源スイッチが、画面の幅にかかわらず常にシミュレーターの左に横並びになり、
アプリの標準テスト幅（390px）で横スクロールが出ないこと**（`.sim-top`/`.sim-panel`/
`.sim-with-switch`に余計な余白を戻すと固定幅211pxの表示部と衝突して横スクロールが出るので注意。
375px以下のごく狭い画面は対象外）、**「進行状況」（右列）に手順チップ・案内メッセージ
（`#sim-msg`）・結果コメント（`#sim-note`）・長押し等の案内文（`.hint`）がまとまって
表示されること**、**`document.body.scrollWidth`だけでなく、`.sim-panel`が
`.card`の枠内に収まっていること・`.sim-screen`（表示部）が`.sim-top`（黒いベゼル）の内側に
収まっていることも（`getBoundingClientRect()`で）確認すること**（横スクロールが出ていなくても、
入れ子の内側だけで静かにあふれる不具合が実際に発生したため。詳細は前項参照）、
**右側に「簡易手順」の文章リストが出て、いま案内中の行がチップと同じタイミングで
ハイライトされること**、「順番に再生」の間隔が読みづらいほど速くないこと、
**`#cmd-play-speed`で秒数を変えると次の再生から間隔が変わること**、
**手順の各行の「＋」や一覧先頭の「＋ 先頭に追加」で、押し忘れたボタンを途中に挿入できること**、
設定タブで追加したコマンドがプルダウンと`settings.json`に反映されることを確認する。
レポートの見方タブは、**インストール情報／システム情報／ECT／MST／MPR／MD／HTの7つが
1本の縦長の印字例に通しで並ぶこと**（章で分けて切り替える方式に戻っていないこと）、
**行にマウスを乗せるとその行の説明**が、
**値にマウスを乗せるとその項目だけの詳しい説明**が出ること（`mouseover`だけでなく**タップ（click）でも
同じように動く**こと）、記号（A・B・C…）付きの固まりで値と「項目一覧」がハイライトで連動し、
**別の固まりの同じ記号は光らないこと**、
**印字例の桁が実機と同じ位置に並ぶこと**（全角を2桁で数える`repWidth()`。桁がずれると見方が変わってしまう）、
狭い画面で説明が印字例の上に貼り付いて、下の方の値をタップしても説明が画面内に見えること、
どの幅でも横スクロールが出ないことを確認する。
**印字例（`#rep-sample`）が白黒の印刷物らしい見た目になっていること**（薄いグレーの台紙の上に
白い紙が乗っている構図で、色を使わず値の下線だけで押せることを示している）、
**「■の行→■で挟んだ見出し→■の行」の3行（`repBannerHtml()`）は先頭の「インストール情報」だけに
出て、システム情報以降で重ねて出ていないこと**（システム情報は平文の見出し行、ECT/MST/MPR/MD/HT設定
には見出し行自体が無いのが実機の印字どおり。テストは`.rg-btitle`の個数が1であることを確認する）、
**マウスを乗せた/タップした値・行が黒塗りではなく蛍光ペンで
マークしたような見た目（黄色の地色＋下線）になっていること**、
**広い画面でどれだけ下までスクロールしても、右の「説明」（`.rg-detail`）が画面内に残り続けること**
（`.rg-body`に`align-items:flex-start`を付けると`.rg-side`の高さが自分の中身だけになり、
印字例より短い分だけスクロールしただけで説明も含めて画面外に流れてしまう不具合が実際に発生した。
`.rg-side`が印字例と同じ高さまで伸びていることを`getBoundingClientRect()`で確認するとよい）、
右側の項目一覧（`.rg-fields-wrap`）は普通に流れて`.rg-side`全体が画面からずれて浮いたりしないこと
（MPRの21項目など項目一覧が長い操作で特に確認する）を確認する。
**「注意事項」（📌カード）は、ロックされていても誰でも一覧が読めること**、**「🔒 追加する」を押すと
その場でパスワード欄が出て、設定タブへ移動しなくてもこの画面から直接ロック解除できること**
（`unlockFromRep()`。誤ったパスワードでは解除されないこと）、**解除すると入力欄と追加ボタンが出て、
追加すると一覧に増え入力欄が空になること・削除ボタン（✕）で消せること**、**タブを離れてまた
レポートタブに戻っても登録した注意事項が残っていること**（`openRepTab()`で`renderRepUserNotes()`を
呼んでいる）、**ここで解除した状態は設定タブ側にもそのまま反映されること**（管理者ロックは
アプリ全体で1つの状態）、**ロックされている状態（他の利用者）では削除ボタンも追加欄も出ないこと**を
確認する。
上部タブは、**600px未満では設定タブが歯車マークだけになり、5つのタブが横スクロールせず収まること**も確認する。
業態ごとの手順書・資料（画像・PDF）は `php -S` 等でPHPを動かした上で、
管理者ロック中はアップロード・削除ができないこと、画像・PDFのアップロードが一覧とサーバー上の
`manuals/` フォルダの両方に反映されること、削除で一覧とサーバー上のファイルの両方から消えること、
作業当日タブで業態を選んだときだけ資料が表示され別の業態では非表示になること、
`settings.json` に保存されて再読み込み後も残ることを確認する。
入力途中の内容が画面の更新で消えないこと（当日タブの業態・店舗名・連絡文・機器台数、カレンダータブの
②の入力）、24時間経った下書きは復元されないこと。
管理者パスワードは、①設定ファイル無し（組み込みハッシュ）②`config.json` を置いた場合
③`admin-auth.php` を置いた場合の3通りで、正しい／誤ったパスワードの挙動を確認する。
`index.html` に平文パスワードが含まれていないこと（`grep -c 'Welsys@1234' index.html` が 0）も確認する。
PWAは `manifest.json` がdata URIでなく実ファイルとして参照でき、アイコンのURLが全て取得でき、
Service Workerが登録されることを確認する（`http://` の簡易サーバーでも登録自体は確認できる）。

## 配置

サーバーのドキュメントルート配下に `index.html` / `manual.html` / `assets/` を同じ階層で置く。
Basic認証を使う場合は `deploy/.htaccess.sample` を参考にする（`AuthUserFile` は配置先の絶対パスに書き換える）。

## 変更を届けるとき（重要）

**利用者はZIPを展開し、新しいファイルだけ上書きする運用をしている。** そのため、
指示の有無にかかわらず、コード変更を行ったら毎回、配置に必要なファイル一式をZIPにまとめて渡すこと
（「ZIPファイル用意して」と言われたときだけ作る、という扱いにしない）。
- 対象: `index.html` `manifest.json` `manual.html` `sw.js` `icon-192.png` `icon-512.png`
  `icon-512-maskable.png` `assets/` `settings-save.php` `admin-auth.php` `manual-upload.php`
  `manual-delete.php` `manual-view.php` `deploy/`（サーバー配置に関係する一式。`CLAUDE.md` `SPEC.md`
  `README.md` `legacy/` `apps-script/` は開発用ドキュメントなので含めない）。
- ZIPの中身は**サーバーのドキュメントルート直下に展開したときの相対パスと同じ階層**にする
  （`oes-app/index.html` のような余計な1段フォルダを作らない。`zip -r ../oes-app.zip index.html
  manifest.json manual.html sw.js icon-*.png assets settings-save.php admin-auth.php
  manual-upload.php manual-delete.php manual-view.php deploy` のように、対象ディレクトリ内から
  直接zip化する）。
- **`settings-save.php` `admin-auth.php` `manual-upload.php` `manual-delete.php` `manual-view.php`
  はリポジトリの直下（index.htmlと同じ階層）に置くこと。`deploy/`の中には戻さない。** これらは秘密情報を
  含まないため、利用者が手動でコピー・リネームしなくてもZIP展開だけでそのまま動く設計にしている
  （以前は`deploy/`のサンプルを手動でコピーする方式だったが、置き忘れによる不具合報告を受けて
  この形に変更した）。一方 `config.php`（管理者パスワードという秘密情報そのもの）と
  `config.json.sample`（そのハッシュ版）は**今後も`deploy/`に置いたまま、自動配布の対象にしないこと**
  （ZIPで自動上書きすると、利用者が独自に設定したパスワードを気づかないうちに消してしまう恐れがあるため）。
- 変更していないファイルの更新日時を上書きしない（`cp` で一旦コピーしてからzip化すると
  更新日時が「今」になってしまい、「新しいものだけ上書き」の判定が壊れるため、**リポジトリの
  ファイルを直接zip化する**。コピーが必要な場合は `cp -p` や `rsync -a` などタイムスタンプを
  保持する方法を使うこと）。
- 渡す前に、zipを展開してローカルサーバーで簡単な動作確認（起動する・JSエラーが無い・
  `manifest.json` のアイコンが全て取得できる等）をしてから渡す。
