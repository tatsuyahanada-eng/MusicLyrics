# 車両移動経費請求書（RouteSearch）変更履歴

バージョンは「Ver 1.5 → 1.6 → 1.7 …」のように、修正・機能追加のたびに 0.1 ずつ上げます。
画面右上の「Ver」表示と、接続テストで表示される「サーバー Ver」で、設置中のバージョンを確認できます。

## Ver 1.5（2026-09-26）

- 領収書AI読み取りを追加（Google Gemini を利用）
  - 高速・駐車場の領収書の画像をまとめてドラッグ＆ドロップすると、AIが高速代か駐車場代かを自動で識別し、金額・道路名・駐車場名・入口→出口を読み取る
  - フォルダごとのドロップ、ETC利用明細のPDFにも対応
  - ダブルチェック（同じ画像を2回読み取って金額を照合）、食い違いや読み取り不安のある行は「要確認」で赤く表示
  - 確認一覧で修正してから「フォームに反映」。AIで入力した欄は黄色で表示し、二重反映を防止
  - 枠の外にドロップしてもブラウザが画像を開いて入力内容が消えないようにした
- APIキーはサーバーの `api/config.php` にのみ保存（ブラウザには渡さない）。書き換えるだけで次の読み取りから反映
- 「🔌 接続テスト」ボタンを追加（APIキー末尾4桁・モデル・サーバー側のバージョンを確認）
- 画面にバージョン表示を追加
- 有料道路・駐車場の表は、必要に応じてExcelテンプレートの上限（8行・7行）まで自動で行を追加

## Ver 1.4 以前（〜2026-09）

- 交通費（車両移動経費）の入力フォーム、JCOMテンプレート準拠のExcel出力
- 住所からの距離自動計算（OSRM）、往復ルート、区間ごとの距離表示、地図プレビュー、Googleマップ表示
- 訪問先の一括貼り付け・ドラッグでの並べ替え、Basic認証（.htaccess / .htpasswd）など
