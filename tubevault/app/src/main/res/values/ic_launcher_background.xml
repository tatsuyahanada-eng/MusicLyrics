<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The icon artwork's own plate colour. The foreground carries the whole
      icon including its plate, so this only has to fill the margin the
      launcher's mask reaches into — matching the plate makes that seam
      invisible whatever shape the mask is.
    -->
    <color name="ic_launcher_background">#FEC904</color>
</resources>
