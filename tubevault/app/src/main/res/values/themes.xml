<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Only used for the window before Compose draws. Painting it the same grey
      as the Compose background stops the launch flashing white first.
    -->
    <style name="Theme.TubeVault" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
